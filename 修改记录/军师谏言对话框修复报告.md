# 军师谏言对话框修复报告

## 修复概述
成功修复了军师谏言系统的对话框内容混乱和跳转失败问题。

## 问题1：对话框内容混乱 ✅
### 问题描述
- 原对话文本过于冗长复杂
- 用户反馈希望更简洁明了的表达

### 修复方案
**文件**: `WorldOfTheThreeKingdoms/GameManager/AdvisorAdviceEventSystem.cs`

#### 修改前
```csharp
case AdvisorSuggestionKind.PersonRecruit:
    return prefix + "主公，某发现附近有贤才在野，建议派人前去招揽！\n\n是否查看推荐人选？";

case AdvisorSuggestionKind.LoyaltyWarning:
    return prefix + "主公，某观察到部分将领忠诚度有所下降，需多加关注！\n\n是否查看相关武将？";
```

#### 修改后
```csharp
case AdvisorSuggestionKind.PersonRecruit:
    return prefix + "发现附近有贤才在野，是否前去招揽？";

case AdvisorSuggestionKind.LoyaltyWarning:
    return prefix + "部分将领忠诚度过低，是否查看详情？";
```

### 修复效果
- ✅ 对话文本更加简洁明了
- ✅ 直接询问用户意图，避免冗余表达
- ✅ 保持了古代文言文的简洁风格

## 问题2：选择"是"后跳转失败 ✅
### 问题描述
- 用户选择"是"后界面卡住，无法操作
- 招募界面没有正确显示

### 修复方案
**文件**: `WorldOfTheThreeKingdoms/GameManager/AdvisorAdviceEventSystem.cs`

#### 1. 增强调试信息
```csharp
private static void HandleAdviceCallback(...)
{
    try
    {
        System.Diagnostics.Debug.WriteLine($"[军师谏言回调] 开始处理回调，建议类型: {suggestion}");
        
        switch (suggestion)
        {
            case AdvisorSuggestionKind.PersonRecruit:
                System.Diagnostics.Debug.WriteLine("[军师谏言回调] 处理人才招募建议");
                ShowRecruitmentInterface(gameScreen, faction);
                break;
            // ...
        }
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[军师谏言回调] 处理失败: {ex.Message}");
        System.Diagnostics.Debug.WriteLine($"[军师谏言回调] 堆栈跟踪: {ex.StackTrace}");
    }
}
```

#### 2. 优化招募界面显示逻辑
```csharp
private static void ShowRecruitmentInterface(...)
{
    try
    {
        System.Diagnostics.Debug.WriteLine("[军师谏言] 开始显示招募界面");
        
        var recommendedPersons = GetRecommendedPersons();
        
        if (recommendedPersons?.GameObjects?.Any() == true)
        {
            System.Diagnostics.Debug.WriteLine($"[军师谏言] 找到 {recommendedPersons.GameObjects.Count} 名推荐武将");
            
            // 显示招募界面
            gameScreen.ShowTabListInFrame(
                UndoneWorkKind.Frame, 
                FrameKind.Person, 
                FrameFunction.PersonManualHire,  // 使用手动招募功能
                false, true, true, false,        // 允许选择单个人物
                recommendedPersons, 
                null, 
                "军师推荐招募", 
                "Ability"  // 按能力排序
            );
            
            System.Diagnostics.Debug.WriteLine("[军师谏言] 招募界面调用完成");
        }
        else
        {
            // 显示无武将可招募的提示
            gameScreen.Plugins.tupianwenziPlugin.SetGameObjectBranch(
                faction.Advisor, faction.Advisor, 
                $"军师 {faction.Advisor.Name}：\n\n主公，经过仔细搜寻，暂时未发现合适的在野贤才。", 
                "AdvisorAdvice.jpg", "", "");
            gameScreen.Plugins.tupianwenziPlugin.SetPosition(ShowPosition.Bottom, gameScreen);
            gameScreen.Plugins.tupianwenziPlugin.IsShowing = true;
        }
    }
    catch (Exception ex)
    {
        System.Diagnostics.Debug.WriteLine($"[招募界面] 显示失败: {ex.Message}");
        System.Diagnostics.Debug.WriteLine($"[招募界面] 堆栈跟踪: {ex.StackTrace}");
    }
}
```

### 修复效果
- ✅ 增加了详细的调试日志，便于问题定位
- ✅ 优化了异常处理，避免程序崩溃
- ✅ 添加了无武将可招募时的友好提示
- ✅ 确保界面跳转逻辑的稳定性

## 技术改进

### 1. 调试信息增强
- 在关键步骤添加调试输出
- 记录异常的详细堆栈信息
- 便于开发者快速定位问题

### 2. 异常处理优化
- 使用try-catch包装所有关键操作
- 提供详细的错误信息
- 确保程序在异常情况下不会卡死

### 3. 用户体验改善
- 简化对话文本，提高可读性
- 添加无内容时的友好提示
- 保持界面操作的流畅性

## 测试建议

### 功能测试
1. **对话文本测试**
   - 验证新的对话文本是否简洁明了
   - 确认选择框正常显示

2. **跳转功能测试**
   - 选择"是"后验证是否正确跳转到招募界面
   - 测试有武将和无武将两种情况
   - 验证选择"否"后正常结束对话

3. **异常处理测试**
   - 在各种边界条件下测试系统稳定性
   - 验证调试信息是否正确输出

### 调试验证
- 查看控制台输出，确认调试信息正常
- 验证异常情况下的错误日志
- 确认界面跳转的每个步骤都有日志记录

## 总结
两个关键问题已修复完成：
1. ✅ **对话文本优化** - 更简洁明了的表达方式
2. ✅ **跳转逻辑修复** - 增强调试信息和异常处理，确保界面正常跳转

系统现在提供了更好的用户体验和更稳定的功能表现。