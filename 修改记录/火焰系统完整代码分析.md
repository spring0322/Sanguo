# 三国志游戏火焰系统完整代码分析

## 概述
游戏中的火焰系统是一个复杂的战术系统，包含火焰的产生、扩散、持续燃烧、伤害计算和视觉效果等多个方面。

## 1. 火焰产生机制

### 1.1 战法触发火焰
游戏中有两个主要的火焰相关战法：

#### 火攻战法 (ID: 4)
- **位置**: `WorldOfTheThreeKingdoms/GameScreens/MGSContextMenu.cs:1083`
- **影响效果**: `InfluenceKind394`
- **实现**: 直接对目标部队造成火焰伤害
```csharp
case ContextMenuResult.TroopStratagem_4:  //火攻
    this.SetTroopStratagem(4);
    break;
```

#### 点火战法 (ID: 8)  
- **位置**: `WorldOfTheThreeKingdoms/GameScreens/MGSContextMenu.cs:1100`
- **影响效果**: `InfluenceKind398`
- **实现**: 在地图上设置火焰点
```csharp
case ContextMenuResult.TroopStratagem_8:  //点火
    this.SetTroopStratagem(8);
    this.PushUndoneWork(new UndoneWorkItem(UndoneWorkKind.Selecting, SelectingUndoneWorkKind.TroopSetFirePosition));
    break;
```

### 1.2 火焰影响效果实现

#### InfluenceKind394 - 火攻效果
**文件**: `WorldOfTheThreeKingdoms/GameObjects/Influences/InfluenceKindPack/InfluenceKind394.cs`
```csharp
public override void ApplyInfluenceKind(Troop troop)
{
    if (troop.GetCurrentStratagemSuccess(troop.OrientationTroop, troop.InevitableHuogongOnLowerIntelligence || troop.InevitableStratagemOnLowerIntelligence, troop.OrientationTroop.InvincibleHuogong, troop.OrientationTroop.InvincibleStratagemFromLowerIntelligence))
    {
        troop.OrientationTroop.SetOnFire(troop.GenerateFireDamageScale(this.scale, Session.Current.Scenario.GetTerrainDetailByPositionNoCheck(troop.OrientationTroop.Position)));
    }
    foreach (Troop troop2 in troop.AreaStratagemTroops)
    {
        if (troop.GetCurrentStratagemSuccess(troop2, troop.InevitableHuogongOnLowerIntelligence || troop.InevitableStratagemOnLowerIntelligence, troop2.InvincibleHuogong, troop2.InvincibleStratagemFromLowerIntelligence))
        {
            troop2.SetOnFire(troop.GenerateFireDamageScale(this.scale, Session.Current.Scenario.GetTerrainDetailByPositionNoCheck(troop2.Position)));
        }
    }
}
```

#### InfluenceKind398 - 点火效果
**文件**: `WorldOfTheThreeKingdoms/GameObjects/Influences/InfluenceKindPack/InfluenceKind398.cs`
```csharp
public override void ApplyInfluenceKind(Troop troop)
{
    if (troop.GetCurrentStratagemSuccess(null, false, false, false))
    {
        Session.Current.Scenario.SetPositionOnFire(troop.SelfCastPosition);
    }
}
```

## 2. 火焰管理系统

### 2.1 火焰表管理
**文件**: `WorldOfTheThreeKingdoms/GameObjects/GameScenario.cs`

#### 设置火焰位置
```csharp
public void SetPositionOnFire(Point position)
{
    this.FireTable.AddPosition(position);
    this.GeneratorOfTileAnimation.AddTileAnimation(TileAnimationKind.火焰, position, true);
}
```

#### 清除火焰位置
```csharp
public void ClearPositionFire(Point position)
{
    this.FireTable.RemovePosition(position);
    this.GeneratorOfTileAnimation.RemoveTileAnimation(TileAnimationKind.火焰, position, true);
}
```

#### 初始化火焰动画
```csharp
public void InitializeFireAnimation()
{
    foreach (Point point in this.FireTable.Positions)
    {
        this.GeneratorOfTileAnimation.AddTileAnimation(TileAnimationKind.火焰, point, true);
    }
}
```

### 2.2 火焰有效性检查
```csharp
public bool IsFireVaild(Point position, bool typevalid, MilitaryType type)
{
    if (this.GetArchitectureByPosition(position) != null)
    {
        return false;
    }
    TerrainKind terrainKindByPosition = this.GetTerrainKindByPosition(position);
    return (((typevalid && (type == MilitaryType.水军)) && (terrainKindByPosition == TerrainKind.水域)) || 
            ((((terrainKindByPosition == TerrainKind.平原) || (terrainKindByPosition == TerrainKind.草原)) || 
              (terrainKindByPosition == TerrainKind.森林)) || (terrainKindByPosition == TerrainKind.山地)));
}
```

## 3. 火焰扩散机制

### 3.1 火焰扩散处理
**文件**: `WorldOfTheThreeKingdoms/GameObjects/GameScenario.cs`
```csharp
public void FireSpread(Point position)
{
    GameArea area = GameArea.GetArea(position, 1, false);
    foreach (Point point in area.Area)
    {
        if ((point != position) && this.IsFireVaild(point, false, MilitaryType.步兵))
        {
            if (this.PositionIsOnFire(point))
            {
                continue;
            }
            int chance = 0;
            switch (this.GetTerrainKindByPosition(position))
            {
                case TerrainKind.平原:
                    chance = 3;
                    break;
                case TerrainKind.草原:
                    chance = 4;
                    break;
                case TerrainKind.森林:
                    chance = 10;
                    break;
                case TerrainKind.山地:
                    chance = 6;
                    break;
            }
            if (GameObject.Chance((int)(chance * Session.Parameters.FireSpreadProbMultiply)))
            {
                this.SetPositionOnFire(point);
                Troop troopByPosition = this.GetTroopByPosition(point);
                if (troopByPosition != null)
                {
                    troopByPosition.BurntBySpreadFire();
                }
            }
        }
    }
}
```

### 3.2 火焰持续燃烧处理
```csharp
// 火焰持续燃烧逻辑
List<Point> list = new List<Point>();
foreach (Point point in this.FireTable.Positions)
{
    if (GameObject.Chance(Session.Parameters.FireStayProb))
    {
        list.Add(point);
    }
}
foreach (Point point in list)
{
    this.ClearPositionFire(point);
}
```

## 4. 火焰伤害系统

### 4.1 部队火焰伤害处理
**文件**: `WorldOfTheThreeKingdoms/GameObjects/Troop.cs`

#### 设置部队着火
```csharp
public void SetOnFire(float scale)
{
    this.ReceiveFireDamage(scale);
    if (Session.Current.Scenario.IsFireVaild(this.Position, false, MilitaryType.步兵))
    {
        Session.Current.Scenario.SetPositionOnFire(this.Position);
    }
}
```

#### 接受火焰伤害
```csharp
public void ReceiveFireDamage(float scale)
{
    int decrement = (int)(this.Army.Kind.MinScale * scale);
    decrement = (int)(decrement * this.FireDamageRate);
    if (this.RateOfFireProtection > 0f)
    {
        decrement = (int)(decrement * this.RateOfFireProtection);
    }
    this.DecreaseQuantity(decrement);
    this.IncreaseInjuryQuantity((decrement * this.InjuryChance) / 100);
    CheckTroopRout(this);
    ExtensionInterface.call("TroopReceiveFireDamage", new Object[] { Session.Current.Scenario, this, decrement });
}
```

#### 火焰扩散伤害
```csharp
public void BurntBySpreadFire()
{
    if (this.OnGetSpreadBurnt != null)
    {
        this.OnGetSpreadBurnt(this, this);
    }
    this.ReceiveFireDamage(Session.Parameters.FireDamageScale * Session.Current.Scenario.GetTerrainDetailByPositionNoCheck(this.Position).FireDamageRate);
}
```

### 4.2 火焰伤害计算
```csharp
public float GenerateFireDamageScale(float baseScale, TerrainDetail td)
{
    return (((baseScale + (((float)this.TroopIntelligence) / 200f)) * this.RateOfFireDamage) * td.FireDamageRate);
}

public float FireDamageRate
{
    get
    {
        if (this.NotAfraidOfFire) return Math.Min(1.0f, this.Army.Kind.FireDamageRate);
        return this.Army.Kind.FireDamageRate;
    }
}
```

## 5. 火焰视觉效果系统

### 5.1 火焰动画类型
**文件**: `WorldOfTheThreeKingdoms/GameObjects/Animations/TileAnimationKind.cs`
```csharp
public enum TileAnimationKind
{
    无,
    暴击,
    攻心,
    扰乱,
    埋伏,
    伏击,
    火焰,    // 火焰动画
    混乱,
    恢复,
    // ... 其他动画类型
    火攻,    // 火攻动画
    // ...
}
```

### 5.2 火焰动画生成器
**文件**: `WorldOfTheThreeKingdoms/GameObjects/Animations/TileAnimationGenerator.cs`
```csharp
public TileAnimation AddTileAnimation(TileAnimationKind kind, Point position, bool looping)
{
    TileAnimation animation = new TileAnimation();
    animation.Kind = kind;
    animation.Looping = looping;
    animation.Position = position;
    int hashCode = animation.GetHashCode();
    if (!this.TileAnimations.ContainsKey(hashCode))
    {
        animation.LinkedAnimation = Session.Current.Scenario.GameCommonData.AllTileAnimations.GetAnimation((int) kind);
        animation.Drawing = true;
        animation.currentFrameIndex = 0;
        animation.currentStayIndex = 0;
        this.TileAnimations.Add(hashCode, animation);
        return animation;
    }
    return null;
}
```

### 5.3 火焰动画渲染
**文件**: `WorldOfTheThreeKingdoms/MapLayers/TileAnimationLayer.cs`
```csharp
public void Draw(Point viewportSize)
{
    foreach (TileAnimation animation in Session.Current.Scenario.GeneratorOfTileAnimation.TileAnimations.Values)
    {
        if ((Setting.Current.GlobalVariables.DrawTroopAnimation && Session.MainGame.mainGameScreen.mainMapLayer.TileInScreen(animation.Position)) && 
            ((Session.GlobalVariables.SkyEye || Session.Current.Scenario.NoCurrentPlayer) || 
             ((Session.Current.Scenario.CurrentPlayer != null) && Session.Current.Scenario.CurrentPlayer.IsPositionKnown(animation.Position))))
        {
            // 渲染火焰动画
        }
    }
    Session.Current.Scenario.GeneratorOfTileAnimation.ClearFinishedAnimation();
}
```

## 6. 火焰系统参数配置

### 6.1 全局参数
**文件**: `WorldOfTheThreeKingdoms/GameGlobal/Parameters.cs`
```csharp
[DataMember]
public int FireStayProb = 20;                    // 火焰持续燃烧机率

[DataMember]
public float FireSpreadProbMultiply = 1f;        // 火焰扩散乘数

[DataMember]
public float FireDamageScale = 1f;               // 火焰伤害率
```

### 6.2 地形火焰参数
**文件**: `WorldOfTheThreeKingdoms/GameObjects/MapDetail/TerrainDetail.cs`
```csharp
[DataMember]
public float FireDamageRate                      // 地形火焰伤害率
{
    get { return this.fireDamageRate; }
    set { this.fireDamageRate = value; }
}
```

### 6.3 兵种火焰参数
**文件**: `WorldOfTheThreeKingdoms/GameObjects/TroopDetail/MilitaryKind.cs`
```csharp
[DataMember]
public float FireDamageRate                      // 兵种受火伤率
{
    get { return this.fireDamageRate; }
    set { this.fireDamageRate = value; }
}
```

## 7. 火焰扩散常量

**文件**: `WorldOfTheThreeKingdoms/GameObjects/GameObjectConsts.cs`
```csharp
public const float FireDamageDivisor = 200f;
public const int FireExtinguishChanceConst = 20;
public const int FireSpreadChanceOnForestConst = 10;      // 森林火焰扩散几率
public const int FireSpreadChanceOnGrasslandConst = 4;    // 草原火焰扩散几率
public const int FireSpreadChanceOnMountainConst = 6;     // 山地火焰扩散几率
public const int FireSpreadChanceOnPlainConst = 3;        // 平原火焰扩散几率
public const int SetFireCreditDivisor = 100;
```

## 8. 用户界面相关

### 8.1 点火区域选择
**文件**: `WorldOfTheThreeKingdoms/GameScreens/MainGameScreen.cs`
```csharp
case SelectingUndoneWorkKind.TroopSetFirePosition:
    if (this.CurrentTroop != null)
    {
        this.selectingLayer.AreaFrameKind = SelectingUndoneWorkKind.TroopSetFirePosition;
        this.selectingLayer.Area = this.CurrentTroop.GetSetFireArea();
    }
    break;
```

### 8.2 火焰区域获取
**文件**: `WorldOfTheThreeKingdoms/GameObjects/Troop.cs`
```csharp
public GameArea GetSetFireArea()
{
    GameArea area = new GameArea();
    foreach (Point point in this.StratagemArea.Area)
    {
        if (Session.Current.Scenario.IsPositionEmpty(point) && 
            Session.Current.Scenario.IsFireVaild(point, false, MilitaryType.步兵))
        {
            area.AddPoint(point);
        }
    }
    return area;
}
```

## 总结

游戏的火焰系统是一个完整的战术系统，包含：

1. **火焰产生**: 通过火攻和点火战法触发
2. **火焰管理**: 火焰表记录所有火焰位置
3. **火焰扩散**: 根据地形类型和扩散参数自动扩散
4. **火焰伤害**: 对处于火焰位置的部队造成持续伤害
5. **视觉效果**: 通过TileAnimation系统显示火焰动画
6. **参数配置**: 丰富的参数系统控制火焰行为

整个系统设计合理，既有战术深度又有视觉表现力，是游戏中重要的战斗机制之一。