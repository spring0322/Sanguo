# 自封官爵传令官显示问题 - 修复方案

**日期：** 2026-03-23  
**状态：** 待实施  

---

## 🎯 修复目标

修复简报系统创建临时 Person 对象导致的弹窗显示错误问题。

---

## 🔧 修复方案

### 方案：修改 xiejinxingjilu 方法签名

**原理：** 不要创建临时 Person 对象，直接传递实际的 Person 对象。

---

## 📝 修改清单

### 修改 1：MGSPersonText.cs - xiejinxingjilu 方法

**文件：** `WorldOfTheThreeKingdoms/GameScreens/MGSPersonText.cs`  
**位置：** 第1303行

**修改前：**
```csharp
public override void xiejinxingjilu(string shijian, string TextResultString, string TextDestinationString, Point point)
{
    Person p = new Person();  // ❌ 创建临时对象
    p.TextResultString = TextResultString;
    p.TextDestinationString = TextDestinationString;
    this.Plugins.GameRecordPlugin.AddBranch(p, shijian, point);
}
```

**修改后：**
```csharp
public override void xiejinxingjilu(Person person, string shijian, string TextResultString, string TextDestinationString, Point point)
{
    // 🔥 关键修复：使用实际的 Person 对象，不要创建临时对象
    // 日期：2026-03-23
    // 原因：临时对象会导致简报系统显示错误的人物（传令官）
    if (person == null)
    {
        throw new ArgumentNullException(nameof(person), "简报记录必须有有效的 Person 对象");
    }
    
    person.TextResultString = TextResultString;
    person.TextDestinationString = TextDestinationString;
    this.Plugins.GameRecordPlugin.AddBranch(person, shijian, point);
}
```

### 修改 2：Screen.cs - xiejinxingjilu 虚方法签名

**文件：** `WorldOfTheThreeKingdoms/GameObjects/Screen.cs`  
**位置：** 第1002行

**修改前：**
```csharp
public virtual void xiejinxingjilu(string shijian, string TextResultString, string TextDestinationString, Point point)
{
}
```

**修改后：**
```csharp
public virtual void xiejinxingjilu(Person person, string shijian, string TextResultString, string TextDestinationString, Point point)
{
}
```

### 修改 3：Faction.cs - 所有调用点

**文件：** `WorldOfTheThreeKingdoms/GameObjects/Faction.cs`

#### 调用点 1：BecomeEmperorLegally（第9671行）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("BecomeEmperorLegally", this.LeaderName,
    guanjueInfo?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "BecomeEmperorLegally", this.LeaderName,
    guanjueInfo?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

#### 调用点 2：SelfBecomeEmperor（第9733行）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("Zili", this.LeaderName,
    guanjueInfo2?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "Zili", this.LeaderName,
    guanjueInfo2?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

#### 调用点 3：Advancement（第9818行）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("shengguan", this.LeaderName,
    gj.Name, this.Leader.Position);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "shengguan", this.LeaderName,
    gj.Name, this.Leader.Position);
```

#### 调用点 4：SelfAdvancement（第9866行）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("Zili", this.LeaderName,
    gj2.Name, this.Leader.Position);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "Zili", this.LeaderName,
    gj2.Name, this.Leader.Position);
```

---

## ✅ 验证步骤

### 步骤 1：编译检查

```bash
# 编译项目，确保没有编译错误
dotnet build
```

### 步骤 2：运行游戏测试

1. 启动游戏
2. 选择一个势力
3. 等待季度事件触发官爵晋升
4. 检查弹窗显示：
   - ✅ 说话人应该是势力君主，不是传令官
   - ✅ 分支名应该是"Zili"或"LeaderAdvancement"
   - ✅ 对话内容应该正确显示

### 步骤 3：检查简报

1. 打开简报面板
2. 查看官爵晋升事件
3. 检查简报中的人物信息：
   - ✅ 应该显示势力君主的名字
   - ✅ 不应该显示"传令官"

---

## 🎯 预期效果

修复后，官爵晋升事件应该：

1. **弹窗显示正确**：
   - 说话人：势力君主（如曹操、刘备等）
   - 头像：君主的头像
   - 对话内容："承蒙各位爱戴，今日孤自封为王。我们齐心协力，共创大业！"

2. **简报记录正确**：
   - 人物：势力君主
   - 事件：官爵晋升
   - 位置：君主所在位置

3. **不再出现传令官**：
   - 不会显示传令官的头像
   - 不会显示传令官的名字
   - 不会显示错误的文本内容

---

## 📋 回归测试清单

修复后需要测试以下场景：

1. ✅ 朝廷授予官爵（Advancement）
2. ✅ 自封官爵（SelfAdvancement）
3. ✅ 合法称帝（BecomeEmperorLegally）
4. ✅ 自封称帝（SelfBecomeEmperor）
5. ✅ 简报系统显示
6. ✅ 年表系统记录

---

## 🚨 注意事项

### 1. 所有调用点都必须修改

**重要：** `xiejinxingjilu` 方法签名改变后，所有调用点都必须添加 `Person` 参数。

**检查方法：**
```bash
# 搜索所有调用点
grep -r "\.xiejinxingjilu\(" --include="*.cs"
```

### 2. Person 参数不能为 null

**重要：** 传递给 `xiejinxingjilu` 的 Person 对象必须是有效的，不能为 null。

**检查方法：**
- 在调用前添加 null 检查
- 或者在方法内部添加 ArgumentNullException

### 3. 兼容性考虑

**重要：** 如果有其他地方也调用了 `xiejinxingjilu`，都需要修改。

**检查方法：**
```bash
# 搜索所有可能的调用点
grep -r "xiejinxingjilu" --include="*.cs"
```

---

**维护者：** Lead Architect  
**最后更新：** 2026-03-23
