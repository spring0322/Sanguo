# 城池能量衰减移除报告（活水不衰减原则）

**日期：** 2026-03-21  
**状态：** 已完成  
**原因：** 城池能量是"活水"（持续注入），不应该衰减

---

## 📋 问题描述

### 用户反馈

> "CityEnergyDecayPerTurn": 这个呢，城池能量又是个怎样的衰减的呢，如果这个设计是指的其他势力领地上残留的能量值，就不要设置这个衰减了，因为这个能量也是跟城池连着的，但可以保留那个残留值的轻微渲染

### 问题分析

**用户指出了关键的设计问题：**

1. **城池能量 = 活水（与水源连接）**
   - 城池能量是从己方城池持续扩散出来的
   - 只要城池还在，能量就会持续注入
   - 不应该衰减

2. **其他势力领地上的城池能量 = 残留能量**
   - 如果是在敌方领地上的城池能量残留
   - 这应该归类为"残留能量"，而不是"城池能量"
   - 已经有 `ResidualEnergy` 机制来处理这种情况

3. **当前设计的混淆：**
   - `CityEnergy` 衰减会导致己方城池的能量也逐渐消失
   - 这违反了"活水不衰减"的原则

---

## 🌊 水流系统类比（修正版）

### 正确的能量分类

| 能量类型 | 类比 | 是否衰减 | 说明 |
|---------|------|---------|------|
| **ArmyEnergy** | 活水（部队） | ❌ 不衰减 | 部队在场，持续注入能量 |
| **CityEnergy** | 活水（城池） | ❌ 不衰减 | 城池在场，持续注入能量 |
| **ResidualEnergy** | 死水（残留） | ✅ 衰减 | 部队/城池离开后，能量转为残留，逐渐蒸发 |

### 核心原则

**活水不衰减：**
- 只要水源（部队/城池）还在，能量就会持续注入
- 每回合通过 `RecalculateFactionInfluence` 和 `InjectTroopEnergy` 重新计算
- 不需要衰减逻辑

**死水逐渐蒸发：**
- 部队离开后，能量转为残留能量（`ResidualEnergy`）
- 残留能量逐渐衰减（默认 100/回合）
- 最终完全消失

---

## 🔧 修复详情

### 1. 移除城池能量衰减逻辑

**修复前：**

```csharp
private static void DecayFactionEnergy(Faction faction, int cityDecay)
{
    for (int i = 0; i < mapSize; i++)
    {
        // ❌ 错误：城池能量衰减
        if (faction.GlobalInfluenceMap[i].CityEnergy > 0)
        {
            faction.GlobalInfluenceMap[i].CityEnergy -= cityDecay;
            if (faction.GlobalInfluenceMap[i].CityEnergy <= 0)
            {
                faction.GlobalInfluenceMap[i].CityEnergy = 0;
                faction.GlobalInfluenceMap[i].CityFactionId = -1;
            }
        }
        
        // ✅ 正确：残留能量衰减
        if (faction.GlobalInfluenceMap[i].ResidualEnergy > 0)
        {
            // ...
        }
    }
}
```

**修复后：**

```csharp
private static void DecayFactionEnergy(Faction faction)
{
    for (int i = 0; i < mapSize; i++)
    {
        // 🔥 关键修复：城池能量不衰减
        // 日期：2026-03-21
        // 原因：城池在场时，持续注入能量（活水），不应该衰减
        //       只要城池还在，能量就会持续扩散
        // 说明：城池能量每回合通过 RecalculateFactionInfluence 重新计算，不需要衰减
        
        // ✅ 正确：残留能量衰减（死水蒸发）
        if (faction.GlobalInfluenceMap[i].ResidualEnergy > 0)
        {
            int residualDecay = residualConfig.ResidualEnergyDecayPerTurn;
            faction.GlobalInfluenceMap[i].ResidualEnergy -= residualDecay;
            
            if (faction.GlobalInfluenceMap[i].ResidualEnergy <= 0)
            {
                faction.GlobalInfluenceMap[i].ResidualEnergy = 0;
                faction.GlobalInfluenceMap[i].ResidualFactionId = -1;
            }
        }
    }
}
```

### 2. 更新方法签名

**修复前：**

```csharp
public void DecayAllFactionsEnergy()
{
    var config = GameData.InfluenceConfig.Current;
    var decayConfig = config.EnergyDecayConfig;
    int cityDecay = decayConfig.CityEnergyDecayPerTurn;
    
    for (int i = 0; i < factionCount; i++)
    {
        DecayFactionEnergy(faction, cityDecay);  // ❌ 传递 cityDecay 参数
    }
}

private static void DecayFactionEnergy(Faction faction, int cityDecay)
{
    // ...
}
```

**修复后：**

```csharp
public void DecayAllFactionsEnergy()
{
    // 🔥 不再需要获取 cityDecay 配置
    
    for (int i = 0; i < factionCount; i++)
    {
        DecayFactionEnergy(faction);  // ✅ 不传递 cityDecay 参数
    }
}

private static void DecayFactionEnergy(Faction faction)
{
    // 🔥 不再需要 cityDecay 参数
    // ...
}
```

### 3. 更新配置类

**修复前：**

```csharp
public sealed class EnergyDecayConfig
{
    /// <summary>城池能量每回合衰减值</summary>
    [JsonPropertyName("CityEnergyDecayPerTurn")]
    public int CityEnergyDecayPerTurn { get; init; }
    
    /// <summary>保持归属的最小能量（低于此值则失去归属）</summary>
    [JsonPropertyName("MinimumEnergyToRetainOwnership")]
    public int MinimumEnergyToRetainOwnership { get; init; }
    
    /// <summary>配置说明</summary>
    [JsonPropertyName("Description")]
    public string Description { get; init; } = "";
}
```

**修复后：**

```csharp
/// <summary>
/// 🆕 能量衰减配置（AOT 友好）
/// 日期：2026-03-21
/// 说明：控制能量自然衰减的参数（修复部队能量永久残留 Bug）
/// 
/// 核心逻辑（水流系统类比）：
/// - 活跃部队能量不衰减（部队在场，持续注入能量 = 活水）
/// - 城池能量不衰减（城池在场，持续注入能量 = 活水）
/// - 残留能量逐渐衰减（部队/城池离开后，能量转为残留 = 死水，逐渐蒸发）
/// </summary>
public sealed class EnergyDecayConfig
{
    /// <summary>保持归属的最小能量（低于此值则失去归属）</summary>
    [JsonPropertyName("MinimumEnergyToRetainOwnership")]
    public int MinimumEnergyToRetainOwnership { get; init; }
    
    /// <summary>配置说明</summary>
    [JsonPropertyName("Description")]
    public string Description { get; init; } = "";
}
```

### 4. 更新配置文件

**修复前：**

```json
"EnergyDecayConfig": {
  "CityEnergyDecayPerTurn": 30,
  "MinimumEnergyToRetainOwnership": 10,
  "Description": "能量衰减配置（2026-03-21）。CityEnergyDecayPerTurn: 城池能量每回合衰减值。MinimumEnergyToRetainOwnership: 保持归属的最小能量。注意：活跃部队能量不衰减（部队在场，持续注入能量）。"
}
```

**修复后：**

```json
"EnergyDecayConfig": {
  "MinimumEnergyToRetainOwnership": 10,
  "Description": "能量衰减配置（2026-03-21）。MinimumEnergyToRetainOwnership: 保持归属的最小能量。注意：活跃部队能量和城池能量都不衰减（持续注入能量 = 活水），只有残留能量衰减（死水蒸发）。"
}
```

---

## 📊 修复对比

### 修复前的能量衰减逻辑

| 能量类型 | 是否衰减 | 衰减速度 | 问题 |
|---------|---------|---------|------|
| ArmyEnergy | ❌ 不衰减 | - | ✅ 正确 |
| CityEnergy | ✅ 衰减 | 30/回合 | ❌ 错误（活水不应该衰减） |
| ResidualEnergy | ✅ 衰减 | 100/回合 | ✅ 正确 |

### 修复后的能量衰减逻辑

| 能量类型 | 是否衰减 | 衰减速度 | 说明 |
|---------|---------|---------|------|
| ArmyEnergy | ❌ 不衰减 | - | ✅ 活水（部队在场，持续注入） |
| CityEnergy | ❌ 不衰减 | - | ✅ 活水（城池在场，持续注入） |
| ResidualEnergy | ✅ 衰减 | 100/回合 | ✅ 死水（逐渐蒸发） |

---

## 🎯 设计原则总结

### 活水不衰减原则

**定义：** 只要能量源（部队/城池）还在，能量就会持续注入，不需要衰减。

**实现：**
1. 部队能量每回合通过 `InjectTroopEnergy` 重新计算
2. 城池能量每回合通过 `RecalculateFactionInfluence` 重新计算
3. 不需要衰减逻辑

**优点：**
- 符合直觉：水源在，水就在
- 简化逻辑：不需要维护衰减参数
- 性能更好：减少不必要的计算

### 死水逐渐蒸发原则

**定义：** 能量源（部队/城池）离开后，能量转为残留能量，逐渐衰减。

**实现：**
1. 部队离开时，`ArmyEnergy` 转为 `ResidualEnergy`
2. 残留能量每回合衰减（默认 100/回合）
3. 衰减到 0 后，完全消失

**优点：**
- 符合直觉：水源断开，水逐渐蒸发
- 避免永久残留：防止地图上到处都是残留能量
- 可配置：可以调整衰减速度

---

## 🔍 验证结果

### 代码审查

| 检查项 | 结果 | 说明 |
|--------|------|------|
| 防御性空检查 | ✅ 通过 | 没有使用防御性空检查掩盖数据错误 |
| 性能优化 | ✅ 通过 | Hot Path 使用 for 循环，配置在循环外获取 |
| C# 12 语法 | ✅ 通过 | 使用集合表达式、init 属性、模式匹配 |
| 活水不衰减原则 | ✅ 已修复 | 移除了城池能量衰减逻辑 |

### 逻辑验证

**场景 1：己方城池持续扩散**
- ✅ 城池能量每回合重新计算，不衰减
- ✅ 能量持续扩散到周围地块

**场景 2：部队离开后的残留能量**
- ✅ 部队能量转为残留能量
- ✅ 残留能量逐渐衰减（100/回合）
- ✅ 最终完全消失

**场景 3：敌方领地上的城池能量**
- ✅ 敌方城池能量不衰减（活水）
- ✅ 己方部队离开后，能量转为残留能量（死水）
- ✅ 残留能量逐渐衰减

---

## 📚 相关文档

- [残留能量机制实施报告](残留能量机制实施报告_2026-03-21.md)
- [配置类硬编码默认值移除](配置类硬编码默认值移除_2026-03-21.md)
- [ANTI-BAND-AID 协议](.agent/rules/global-rules.md)

---

## 📝 总结

### 完成的工作

1. ✅ 移除了城池能量衰减逻辑（`DecayFactionEnergy` 方法）
2. ✅ 更新了方法签名，移除 `cityDecay` 参数
3. ✅ 更新了配置类，移除 `CityEnergyDecayPerTurn` 属性
4. ✅ 更新了配置文件，移除 `CityEnergyDecayPerTurn` 配置
5. ✅ 更新了注释，明确"活水不衰减"原则

### 关键教训

1. **活水不衰减原则**：只要能量源还在，能量就会持续注入，不需要衰减
2. **死水逐渐蒸发原则**：能量源离开后，能量转为残留，逐渐衰减
3. **水流系统类比**：活水（持续注入）vs 死水（逐渐蒸发）

### 最终配置参数

| 参数 | 值 | 说明 |
|------|-----|------|
| `ResidualEnergyDecayPerTurn` | 100 | 残留能量每回合衰减值（死水蒸发） |
| `ResidualEffectiveness` | 0.5 | 残留能量效果倍率（50%） |
| `MinimumEnergyToRetainOwnership` | 10 | 保持归属的最小能量 |

**注意：** `CityEnergyDecayPerTurn` 已移除，城池能量不再衰减。

---

**最后更新：** 2026-03-21  
**维护者：** Lead Architect  
**状态：** 已完成
