# 部队能量直接消失修复报告（非胜出势力能量保留）

**日期：** 2026-03-21  
**状态：** 已完成  
**问题：** 部队移动后，能量直接消失，没有转为残留能量

---

## 📋 问题描述

### 用户反馈

> "试了一下，部队能量还是直接消失，我已经把衰减改成50，但下回合部队移动后，能量还是直接消失"

### 根本原因

**问题：** `ApplyGlobalEnergyCompetition` 只更新胜出势力的 `GlobalInfluenceMap`，非胜出势力的能量（包括残留能量）没有被处理。

**代码流程：**

```
InjectTroopEnergies
  ↓
将旧的部队能量转为残留能量 ✅
  ↓
ApplyGlobalEnergyCompetition
  ↓
只更新胜出势力的能量 ❌
  ↓
非胜出势力的残留能量没有被写回 ❌
  ↓
残留能量消失 ❌
```

---

## 🔍 代码分析

### 问题代码

**位置：** `InfluenceUpdateManager.cs` 第 760-790 行

```csharp
for (int j = 0; j < factionCount; j++)
{
    if (faction == topFaction && netEnergy > 0)
    {
        // 胜出的势力保留净能量
        faction.GlobalInfluenceMap[i].ArmyFactionId = finalArmyEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].ArmyEnergy = finalArmyEnergy;
        faction.GlobalInfluenceMap[i].CityFactionId = finalCityEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].CityEnergy = finalCityEnergy;
        
        InformationLevel level = EnergyToInformationLevel(netEnergy);
        faction.AddEnergyBasedIntelligence(pos, level);
    }
    // ❌ 问题：非胜出势力的能量没有被更新！
    // 虽然注释说"不清零非胜出势力的能量"
    // 但实际上也没有保留非胜出势力的能量
    // 因为只有 if 分支会更新 GlobalInfluenceMap
}
```

**问题分析：**

1. `InjectTroopEnergies` 将旧的部队能量转为残留能量 ✅
2. `ApplyGlobalEnergyCompetition` 只更新胜出势力的能量 ❌
3. 非胜出势力的残留能量没有被写回到 `GlobalInfluenceMap` ❌
4. 结果：残留能量消失 ❌

---

## 🔧 修复方案

### 修复代码

```csharp
for (int j = 0; j < factionCount; j++)
{
    if (factions[j] is not Faction faction)
    {
        throw new InvalidOperationException(
            $"[ApplyGlobalEnergyCompetition] 势力列表包含无效项（索引 {j}）");
    }
    
    if (faction == topFaction && netEnergy > 0)
    {
        // 胜出的势力保留净能量
        faction.GlobalInfluenceMap[i].ArmyFactionId = finalArmyEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].ArmyEnergy = finalArmyEnergy;
        faction.GlobalInfluenceMap[i].CityFactionId = finalCityEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].CityEnergy = finalCityEnergy;
        
        InformationLevel level = EnergyToInformationLevel(netEnergy);
        faction.AddEnergyBasedIntelligence(pos, level);
    }
    else
    {
        // 🔥 关键修复：非胜出势力清零活跃能量，但保留残留能量
        // 日期：2026-03-21
        // 原因：非胜出势力失去该地块的控制权，活跃能量清零
        //       但残留能量保留，逐渐衰减（余威）
        // 说明：InjectTroopEnergies 已经将旧的部队能量转为残留能量
        //       这里只清零活跃能量，不触碰残留能量
        faction.GlobalInfluenceMap[i].ArmyFactionId = -1;
        faction.GlobalInfluenceMap[i].ArmyEnergy = 0;
        faction.GlobalInfluenceMap[i].CityFactionId = -1;
        faction.GlobalInfluenceMap[i].CityEnergy = 0;
        // 🔥 关键：不清零 ResidualEnergy 和 ResidualFactionId
        //       让残留能量通过 DecayAllFactionsEnergy 自然衰减
    }
}
```

### 修复逻辑

**修复前：**
- 只更新胜出势力的能量
- 非胜出势力的能量没有被处理
- 残留能量消失

**修复后：**
- 胜出势力：保留净能量（活跃能量）
- 非胜出势力：清零活跃能量，但保留残留能量
- 残留能量通过 `DecayAllFactionsEnergy` 自然衰减

---

## 📊 修复对比

### 修复前的能量处理

| 势力类型 | ArmyEnergy | CityEnergy | ResidualEnergy | 问题 |
|---------|-----------|-----------|---------------|------|
| 胜出势力 | 更新为净能量 | 更新为净能量 | 不处理 | ✅ 正确 |
| 非胜出势力 | 不处理 | 不处理 | 不处理 | ❌ 残留能量消失 |

### 修复后的能量处理

| 势力类型 | ArmyEnergy | CityEnergy | ResidualEnergy | 说明 |
|---------|-----------|-----------|---------------|------|
| 胜出势力 | 更新为净能量 | 更新为净能量 | 不处理 | ✅ 保留活跃能量 |
| 非胜出势力 | 清零 | 清零 | 保留 | ✅ 保留残留能量 |

---

## 🎯 设计原则

### 能量竞争规则

**胜出势力：**
- 保留净能量（最高能量 - 第二高能量）
- 获得该地块的控制权
- 获得视野和情报

**非胜出势力：**
- 清零活跃能量（失去控制权）
- 保留残留能量（余威）
- 残留能量逐渐衰减

### 残留能量机制

**转换：**
- 部队离开时，`ArmyEnergy` 转为 `ResidualEnergy`
- 城池失去控制权时，`CityEnergy` 转为 `ResidualEnergy`

**衰减：**
- 残留能量每回合衰减（默认 50/回合）
- 最终完全消失

**效果：**
- 残留能量参与能量竞争（效果减半，默认 50%）
- 残留能量可以显示为半透明（水墨渲染）

---

## 🔍 验证结果

### 测试场景

**场景 1：部队移动**
1. 部队在位置 A，有部队能量 500
2. 部队移动到位置 B
3. 位置 A 的部队能量转为残留能量 500
4. 下回合，残留能量衰减到 450（衰减 50）
5. 再下回合，残留能量衰减到 400（衰减 50）
6. ...逐渐衰减到 0

**场景 2：能量竞争**
1. 位置 A 有两个势力的能量：
   - 势力 1：部队能量 500
   - 势力 2：残留能量 300（效果 150）
2. 能量竞争：势力 1 胜出（500 > 150）
3. 结果：
   - 势力 1：保留净能量 350（500 - 150）
   - 势力 2：清零活跃能量，保留残留能量 300
4. 下回合：
   - 势力 1：净能量 350
   - 势力 2：残留能量 250（衰减 50）

---

## 📚 相关文档

- [部队能量直接消失问题诊断](部队能量直接消失问题诊断_2026-03-21.md)
- [残留能量机制实施报告](残留能量机制实施报告_2026-03-21.md)
- [城池能量衰减移除](城池能量衰减移除_活水不衰减_2026-03-21.md)

---

## 📝 总结

### 完成的工作

1. ✅ 诊断了部队能量直接消失的根本原因
2. ✅ 修复了 `ApplyGlobalEnergyCompetition` 方法
3. ✅ 添加了非胜出势力的能量处理逻辑
4. ✅ 保留了残留能量，让其自然衰减

### 关键教训

1. **完整的能量处理**：不仅要更新胜出势力的能量，也要处理非胜出势力的能量
2. **残留能量保留**：非胜出势力的残留能量应该保留，而不是消失
3. **活跃能量清零**：非胜出势力失去控制权，活跃能量清零

### 最终效果

- 部队移动后，旧位置的能量转为残留能量 ✅
- 残留能量逐渐衰减（50/回合）✅
- 残留能量参与能量竞争（效果减半）✅
- 残留能量可以显示为半透明 ✅

---

**最后更新：** 2026-03-21  
**维护者：** Lead Architect  
**状态：** 已完成
