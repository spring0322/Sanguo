# 部队能量直接消失问题诊断报告

**日期：** 2026-03-21  
**状态：** 诊断中  
**问题：** 部队移动后，能量直接消失，没有转为残留能量

---

## 🔍 问题描述

### 用户反馈

> "试了一下，部队能量还是直接消失，我已经把衰减改成50，但下回合部队移动后，能量还是直接消失"

### 预期行为

根据"活水不衰减"原则：
1. 部队移动前，旧位置有部队能量（`ArmyEnergy`）
2. 部队移动后，旧位置的部队能量应该转为残留能量（`ResidualEnergy`）
3. 残留能量逐渐衰减（50/回合）
4. 最终完全消失

### 实际行为

部队移动后，旧位置的能量直接消失，没有转为残留能量。

---

## 🔍 代码追踪

### 关键流程

```
回合结束
  ↓
ForceUpdateAll()  // 触发分帧重算
  ↓
MarkAllFactionsDirty()  // 标记所有势力需要重算
  ↓
UpdateFactionRecalculation()  // 分帧重算（每帧 1 个势力）
  ↓
RecalculateFactionInfluence(faction)  // 重算单个势力
  ↓
InjectTroopEnergies(faction)  // 注入部队能量
  ↓
ApplyGlobalEnergyCompetition()  // 全局能量竞争
```

### 关键代码 1：InjectTroopEnergies

**位置：** `InfluenceUpdateManager.cs` 第 440-510 行

```csharp
private static void InjectTroopEnergies(Faction faction)
{
    // 🆕 关键修复：先将旧的部队能量转为残留能量
    // 日期：2026-03-21
    for (int i = 0; i < faction.GlobalInfluenceMap.Length; i++)
    {
        // 只处理该势力的部队能量（不影响其他势力）
        // 🔥 关键：ID >= 0 是有效的（ID=0 是洛阳）
        if (faction.GlobalInfluenceMap[i].ArmyFactionId == faction.ID)
        {
            // 转为残留能量（而不是直接清零）
            faction.GlobalInfluenceMap[i].ResidualFactionId = faction.ID;
            faction.GlobalInfluenceMap[i].ResidualEnergy = faction.GlobalInfluenceMap[i].ArmyEnergy;
            
            // 清空活跃能量
            faction.GlobalInfluenceMap[i].ArmyFactionId = -1;
            faction.GlobalInfluenceMap[i].ArmyEnergy = 0;
        }
    }
    
    // 然后重新注入当前部队的能量
    for (int i = 0; i < troopCount; i++)
    {
        MultiSourceInfluenceCalculator.InjectTroopEnergy(faction, troop);
    }
}
```

**分析：** 这段代码看起来是正确的，应该会将旧的部队能量转为残留能量。

### 关键代码 2：ApplyGlobalEnergyCompetition

**位置：** `InfluenceUpdateManager.cs` 第 760-790 行

```csharp
for (int j = 0; j < factionCount; j++)
{
    if (faction == topFaction && netEnergy > 0)
    {
        // 胜出的势力保留净能量
        faction.GlobalInfluenceMap[i].ArmyFactionId = finalArmyEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].ArmyEnergy = finalArmyEnergy;
        faction.GlobalInfluenceMap[i].CityFactionId = finalCityEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].CityEnergy = finalCityEnergy;
        
        // 获得视野和情报
        InformationLevel level = EnergyToInformationLevel(netEnergy);
        faction.AddEnergyBasedIntelligence(pos, level);
    }
    // 🔥 修复：不清零非胜出势力的能量，让能量自然衰减
    // 日期：2026-03-21
    // 原因：部队离开后，能量应该逐渐衰减（余威），而不是立即清零
    // 解决：移除清零逻辑，能量通过 DecayAllFactionsEnergy 自然衰减
}
```

**分析：** 这段代码也是正确的，没有清零非胜出势力的能量。

---

## 🚨 发现的问题

### 问题：ApplyGlobalEnergyCompetition 只更新胜出势力的能量

**关键代码：**

```csharp
if (faction == topFaction && netEnergy > 0)
{
    // 胜出的势力保留净能量
    faction.GlobalInfluenceMap[i].ArmyFactionId = finalArmyEnergy > 0 ? faction.ID : -1;
    faction.GlobalInfluenceMap[i].ArmyEnergy = finalArmyEnergy;
    faction.GlobalInfluenceMap[i].CityFactionId = finalCityEnergy > 0 ? faction.ID : -1;
    faction.GlobalInfluenceMap[i].CityEnergy = finalCityEnergy;
}
// ❌ 问题：非胜出势力的能量没有被更新！
```

**问题分析：**

1. `InjectTroopEnergies` 将旧的部队能量转为残留能量 ✅
2. `ApplyGlobalEnergyCompetition` 只更新胜出势力的能量 ❌
3. **非胜出势力的残留能量没有被写回到 `GlobalInfluenceMap`** ❌

**根本原因：**

`ApplyGlobalEnergyCompetition` 方法中，只有胜出势力的能量被更新到 `GlobalInfluenceMap`，非胜出势力的能量（包括残留能量）没有被处理。

虽然注释说"不清零非胜出势力的能量"，但实际上**也没有保留非胜出势力的能量**，因为只有 `if (faction == topFaction && netEnergy > 0)` 分支会更新 `GlobalInfluenceMap`。

---

## 🔧 修复方案

### 方案 1：保留非胜出势力的残留能量

在 `ApplyGlobalEnergyCompetition` 方法中，对于非胜出势力，保留其残留能量（不更新 `ArmyEnergy` 和 `CityEnergy`，只保留 `ResidualEnergy`）。

**修复代码：**

```csharp
for (int j = 0; j < factionCount; j++)
{
    if (factions[j] is not Faction faction)
    {
        throw new InvalidOperationException(
            $"[ApplyGlobalEnergyCompetition] 势力列表包含无效项（索引 {j}）");
    }
    
    if (faction == topFaction && netEnergy > 0)
    {
        // 胜出的势力保留净能量
        faction.GlobalInfluenceMap[i].ArmyFactionId = finalArmyEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].ArmyEnergy = finalArmyEnergy;
        faction.GlobalInfluenceMap[i].CityFactionId = finalCityEnergy > 0 ? faction.ID : -1;
        faction.GlobalInfluenceMap[i].CityEnergy = finalCityEnergy;
        
        // 获得视野和情报
        InformationLevel level = EnergyToInformationLevel(netEnergy);
        faction.AddEnergyBasedIntelligence(pos, level);
    }
    else
    {
        // 🔥 关键修复：非胜出势力清零活跃能量，但保留残留能量
        // 日期：2026-03-21
        // 原因：非胜出势力失去该地块的控制权，活跃能量清零
        //       但残留能量保留，逐渐衰减（余威）
        faction.GlobalInfluenceMap[i].ArmyFactionId = -1;
        faction.GlobalInfluenceMap[i].ArmyEnergy = 0;
        faction.GlobalInfluenceMap[i].CityFactionId = -1;
        faction.GlobalInfluenceMap[i].CityEnergy = 0;
        // 🔥 关键：不清零 ResidualEnergy 和 ResidualFactionId
        //       让残留能量通过 DecayAllFactionsEnergy 自然衰减
    }
}
```

### 方案 2：在能量竞争前不清零残留能量

确保 `InjectTroopEnergies` 转换的残留能量不会被 `ApplyGlobalEnergyCompetition` 清零。

---

## 🎯 根本原因总结

**问题：** `ApplyGlobalEnergyCompetition` 只更新胜出势力的 `GlobalInfluenceMap`，非胜出势力的能量（包括残留能量）没有被处理。

**表现：** 部队移动后，旧位置的能量直接消失。

**原因：**
1. `InjectTroopEnergies` 将旧的部队能量转为残留能量 ✅
2. `ApplyGlobalEnergyCompetition` 只更新胜出势力的能量 ❌
3. 非胜出势力的残留能量没有被写回，导致消失 ❌

**修复：** 在 `ApplyGlobalEnergyCompetition` 中，对于非胜出势力，清零活跃能量，但保留残留能量。

---

**最后更新：** 2026-03-21  
**维护者：** Lead Architect  
**状态：** 已诊断，待修复
