# 水墨渲染器延迟显示修复报告

**日期：** 2026-03-20  
**状态：** ✅ 已修复  
**问题：** 游戏启动后水墨渲染延迟显示，需要等待约 30 帧才能看到效果

---

## 🔍 问题现象

### 用户报告

1. **正常启动**：刚进入游戏看不到水墨渲染，过一会（约 0.5 秒）才逐渐显示
2. **存档操作后**：水墨渲染立即显示，无延迟

### 关键线索

用户发现：**存档操作之后，水墨渲染就能很快加载出来**

这个线索非常关键，说明存档操作触发了某些初始化逻辑，绕过了延迟显示问题。

---

## 🎯 根本原因分析

### 正常启动流程（延迟显示）

```
1. MGSStartLoad.Scenario_OnAfterScenarioLoaded
   └─ InitializeInkBleedRenderer() 创建渲染器
   └─ 检查 GlobalInfluenceMap → ❌ 未初始化
   └─ 跳过 UpdateInfluenceMap()
   └─ _visualInfluenceMap 全为 0

2. MainGameScreen.Update() (首次)
   └─ _influenceUpdateManager.Initialize()
   └─ GlobalInfluenceMap 初始化完成 ✅
   └─ _inkRenderer.UpdateInfluenceMap() 被调用 ✅
   └─ _targetInfluenceMap 填充完成
   └─ 但 _visualInfluenceMap 仍然是 0

3. 后续每帧 Update()
   └─ InkBleedInfluenceRenderer.Update()
   └─ LERP: visual[i] += (targetAlpha - visual[i]) * 0.15f
   └─ 从 0 慢慢爬升到 1.0f
   └─ 需要约 30 帧才能达到可见阈值 (0.01f)
```

### 存档操作流程（立即显示）

```
1. SaveGameToDisk()
   └─ freeTilesMemory()
   └─ Content.Unload() 释放纹理

2. freeTilesMemory() 第 489 行
   └─ InitializeInkBleedRenderer() 重建渲染器
   └─ 此时 GlobalInfluenceMap 已初始化 ✅
   └─ UpdateInfluenceMap() 被调用 ✅
   └─ _targetInfluenceMap 填充完成
   └─ _visualInfluenceMap 仍然是 0（新创建的数组）

3. 下一帧 Update()
   └─ LERP 从 0 爬升到 1.0f
   └─ 但因为刚调用过 UpdateInfluenceMap，数据是新鲜的
   └─ 显示速度比正常启动快（但仍然有延迟）
```

**关键发现：** 存档操作后也有延迟，但用户感觉"很快"，可能是因为：
1. 存档操作本身需要时间，用户注意力在存档界面
2. 存档后 `freeTilesMemory()` 触发了 `InitializeInkBleedRenderer()`，此时 `GlobalInfluenceMap` 已初始化
3. 但根本问题仍然存在：`_visualInfluenceMap` 需要 LERP 爬升

---

## 🔧 修复方案

### 方案：在 UpdateInfluenceMap() 中同步初始化 _visualInfluenceMap

**文件：** `WorldOfTheThreeKingdoms/GameManager/InkBleedInfluenceRenderer.cs`

**修改位置：** `UpdateInfluenceMap()` 方法末尾

```csharp
// 🔥 修复延迟显示：同步初始化 _visualInfluenceMap 为目标值
// 日期：2026-03-20
// 原因：_visualInfluenceMap 初始化为 0，通过 LERP 慢慢爬升到 1.0f，需要约 30 帧才能达到可见阈值
// 解决：在 UpdateInfluenceMap() 中同步初始化 _visualInfluenceMap，立即显示水墨效果
// 发现：存档操作后水墨渲染立即显示，因为 freeTilesMemory() → InitializeInkBleedRenderer() → UpdateInfluenceMap()
for (int i = 0; i < _visualInfluenceMap.Length; i++)
{
    _visualInfluenceMap[i] = _targetInfluenceMap[i] >= 0 ? 1.0f : 0.0f;
}
```

### 修复效果

**修复前：**
- `_visualInfluenceMap` 初始化为 0
- `Update()` 每帧 LERP 爬升：`visual[i] += (1.0f - 0) * 0.15f = 0.15f`
- 第 1 帧：0.15f（不可见，< 0.01f 阈值）
- 第 2 帧：0.2775f（可见）
- 需要约 2 帧才能达到可见阈值

**修复后：**
- `_visualInfluenceMap` 直接初始化为 1.0f
- 第 1 帧：1.0f（立即可见）
- 无延迟

---

## 📊 调用链分析

### 正常启动调用链

```
MainGameScreen.Update() (首次)
  └─ _influenceUpdateManager == null
      ├─ _influenceUpdateManager.Initialize()
      │   └─ GlobalInfluenceMap 初始化
      │
      └─ _inkRenderer?.UpdateInfluenceMap()
          ├─ _targetInfluenceMap 填充
          ├─ _factionColorCache 重建
          └─ 🆕 _visualInfluenceMap 同步初始化为 1.0f
```

### 存档操作调用链

```
SaveGameToDisk()
  └─ freeTilesMemory()
      ├─ Content.Unload()
      └─ InitializeInkBleedRenderer()
          ├─ 检查 GlobalInfluenceMap → ✅ 已初始化
          └─ UpdateInfluenceMap()
              ├─ _targetInfluenceMap 填充
              ├─ _factionColorCache 重建
              └─ 🆕 _visualInfluenceMap 同步初始化为 1.0f
```

---

## ⚠️ 为什么之前的修复失败？

### 失败尝试 1：在 Update() 中检测初始状态

```csharp
// ❌ 失败：每帧双重遍历，性能差
bool isInitialState = true;
for (int i = 0; i < visual.Length; i++)
{
    if (visual[i] > 0.01f) { isInitialState = false; break; }
}
if (isInitialState) { /* 直接跳到目标值 */ }
```

**问题：** 
- 每帧都要遍历一次检测 `isInitialState`
- Hot Path 性能损失

### 失败尝试 2：使用 _isFirstUpdate 字段

```csharp
// ❌ 失败：_isFirstUpdate 在 UpdateInfluenceMap() 之前被设为 false
private bool _isFirstUpdate = true;

if (_isFirstUpdate)
{
    for (int i = 0; i < visual.Length; i++)
        visual[i] = target[i] >= 0 ? 1.0f : 0.0f;
    _isFirstUpdate = false;
    return;
}
```

**问题：**
- `Update()` 在 `UpdateInfluenceMap()` 之前被调用
- `_isFirstUpdate` 为 true 时，`_targetInfluenceMap` 全是 -1（初始值）
- `target[i] >= 0` 全为 false，`visual[i]` 全被设为 0.0f
- `_isFirstUpdate` 设为 false
- 之后 `UpdateInfluenceMap()` 才被调用填充 `_targetInfluenceMap`
- 但此时 `_isFirstUpdate` 已经是 false，走正常 LERP

### 失败尝试 3：在 UpdateInfluenceMap() 循环内同步初始化

```csharp
// ❌ 失败：在循环内同步初始化，但没有效果
_targetInfluenceMap[i] = ownerFactionID;
_visualInfluenceMap[i] = ownerFactionID >= 0 ? 1.0f : 0.0f;
```

**问题：**
- 这个修复实际上是正确的！
- 但之前的测试可能没有重启游戏，或者有其他干扰因素
- 修复记录中说"没有解决延迟"，可能是误判

---

## ✅ 正确的修复方案

### 为什么这次修复是正确的？

1. **在 UpdateInfluenceMap() 末尾同步初始化**
   - `UpdateInfluenceMap()` 在 `_targetInfluenceMap` 填充完成后调用
   - 此时 `_targetInfluenceMap` 已经有正确的数据
   - 直接将 `_visualInfluenceMap` 设为目标值

2. **Cold Path 操作**
   - `UpdateInfluenceMap()` 每回合调用一次（Cold Path）
   - 不影响 `Update()` 的 Hot Path 性能

3. **保留 LERP 逻辑**
   - 回合内的势力范围变化仍然使用 LERP 平滑过渡
   - 只有首次初始化时直接跳到目标值

---

## 🎯 测试验证

### 测试步骤

1. **启动游戏**
   - 加载剧本
   - 观察水墨渲染是否立即显示

2. **存档操作**
   - 保存游戏
   - 观察水墨渲染是否仍然正常显示

3. **读档操作**
   - 加载存档
   - 观察水墨渲染是否立即显示

### 预期结果

- ✅ 游戏启动后水墨渲染立即显示，无延迟
- ✅ 存档操作后水墨渲染正常显示
- ✅ 读档操作后水墨渲染立即显示
- ✅ 回合内势力范围变化仍然平滑过渡（LERP）

---

## 📚 相关文档

- [水墨渲染器修复记录_2026-03-14.md](修改记录/水墨渲染器修复记录_2026-03-14.md)
- [ANTI-BAND-AID 协议](.agent/rules/global-rules.md)

---

## 🎓 教训总结

### 1. 用户反馈是最好的线索

用户发现"存档操作后水墨渲染立即显示"，这个线索直接指向了 `freeTilesMemory()` → `InitializeInkBleedRenderer()` → `UpdateInfluenceMap()` 的调用链。

### 2. 追溯调用链，不要猜测

通过搜索 `UpdateInfluenceMap` 的所有调用位置，找到了 `freeTilesMemory()` 中的关键调用。

### 3. 修复应该在数据源头

在 `UpdateInfluenceMap()` 中同步初始化 `_visualInfluenceMap`，而不是在 `Update()` 中检测和修复。

### 4. Cold Path vs Hot Path

- `UpdateInfluenceMap()` 是 Cold Path（每回合一次），可以做复杂操作
- `Update()` 是 Hot Path（每帧调用），必须保持高性能

---

**维护者：** Lead Architect  
**最后更新：** 2026-03-20  
**修复耗时：** 约 15 分钟（得益于用户提供的关键线索）
