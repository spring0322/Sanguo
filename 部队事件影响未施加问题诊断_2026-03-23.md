# 部队事件影响未施加问题诊断

**日期：** 2026-03-23  
**问题：** 部队事件发生后，对应的影响（Effect）并没有正确施加

---

## 🔍 问题分析

### 事件流程梳理

#### 1. 事件触发阶段（Troop.cs 第 6770-6792 行）

```csharp
// 部队移动时检查事件
if (event2.CheckTroop(this))
{
    // 🔥 关键步骤 1：将事件和部队添加到待处理队列
    TroopList list = null;
    if (Session.Current.Scenario.TroopEventsToApply.TryGetValue(event2, out list))
    {
        list.Add(this);
    }
    else
    {
        list = new TroopList();
        list.Add(this);
        Session.Current.Scenario.TroopEventsToApply.Add(event2, list);
    }
    
    // 🔥 关键步骤 2：显示事件对话
    event2.ApplyEventDialogs(this);
}
```

#### 2. 对话显示阶段（TroopEvent.cs 第 96-113 行）

```csharp
public void ApplyEventDialogs(Troop troop)
{
    #if DEBUG
    System.Diagnostics.Debug.WriteLine($"[TroopEvent.ApplyEventDialogs] TroopEvent ID={this.ID}, Name={this.Name}, Troop={troop.DisplayName}, OnApplyTroopEvent订阅数={(this.OnApplyTroopEvent?.GetInvocationList().Length ?? 0)}");
    #endif
    
    // 🔥 触发 OnApplyTroopEvent 事件（由 MainGameScreen 订阅）
    if (this.OnApplyTroopEvent != null)
    {
        this.OnApplyTroopEvent(this, troop);
    }
    else
    {
        #if DEBUG
        System.Diagnostics.Debug.WriteLine($"[TroopEvent.ApplyEventDialogs] ⚠️ OnApplyTroopEvent 为 null！事件未订阅！");
        #endif
    }
}
```

#### 3. UI 显示阶段（MainGameScreen.cs 第 10098-10130 行）

```csharp
public override void TroopApplyTroopEvent(TroopEvent te, Troop troop)
{
    if ((((Session.Current.Scenario.CurrentPlayer == null) || Session.Current.Scenario.CurrentPlayer.IsPositionKnown(troop.Position)) || Session.GlobalVariables.SkyEye) && (te.Dialogs.Count > 0))
    {
        // 显示对话...
        
        if (Setting.Current.GlobalVariables.DialogShowTime > 0)
        {
            // 🔥 关键：对话关闭后调用 ApplyTroopEvents
            this.Plugins.tupianwenziPlugin.SetCloseFunction(new GameDelegates.VoidFunction(Session.Current.Scenario.ApplyTroopEvents));
            this.Plugins.tupianwenziPlugin.IsShowing = true;
        }
        else
        {
            // 🔥 关键：无对话时立即调用 ApplyTroopEvents
            Session.Current.Scenario.ApplyTroopEvents();
        }
    }
    else
    {
        // 🔥 关键：无对话或不可见时立即调用 ApplyTroopEvents
        Session.Current.Scenario.ApplyTroopEvents();
    }
}
```

#### 4. 影响施加阶段（GameScenario.cs 第 1051-1080 行）

```csharp
public void ApplyTroopEvents()
{
    if (this.TroopEventsToApply.Count != 0)
    {
        foreach (GameObject gameObj in this.TroopEvents)
        {
            TroopEvent event2 = gameObj as TroopEvent;
            if (event2 == null)
            {
                continue;
            }

            TroopList list = null;
            if (this.TroopEventsToApply.TryGetValue(event2, out list))
            {
                // 🔥 关键：遍历所有触发该事件的部队，施加影响
                foreach (GameObject obj in list.GetList())
                {
                    Troop troop = (obj is Troop ? (Troop)obj : null);
                    if (troop != null)
                    {
                        event2.ApplyEventEffects(troop);
                    }
                }
            }
        }
        this.TroopEventsToApply.Clear();
    }
}
```

#### 5. 实际影响施加（TroopEvent.cs 第 114-262 行）

```csharp
public void ApplyEventEffects(Troop self)
{
    if (((self != null) && !self.Destroyed) && (!this.Happened || this.Repeatable))
    {
        this.Happened = true;
        
        // 🔥 施加自身影响
        if (this.SelfEffects.Count > 0)
        {
            foreach (GameObjects.TroopDetail.EventEffect.EventEffect effect in this.SelfEffects)
            {
                effect.ApplyEffect(self.Leader);
            }
        }
        
        // 🔥 施加指定武将影响
        foreach (TroopEffectPerson person in this.EffectPersons)
        {
            person.Effect.ApplyEffect(person.EffectPerson);
        }
        
        // 🔥 施加区域影响（视野敌军、八格友军等）
        // ...
    }
}
```

---

## 🚨 可能的问题点

### 问题 1：OnApplyTroopEvent 未订阅

**症状：**
- 事件触发，但对话不显示
- `ApplyTroopEvents()` 从未被调用
- 影响永远不会施加

**诊断：**
```csharp
// 在 TroopEvent.ApplyEventDialogs 中添加日志
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[TroopEvent.ApplyEventDialogs] OnApplyTroopEvent订阅数={(this.OnApplyTroopEvent?.GetInvocationList().Length ?? 0)}");
#endif
```

**原因：**
- `TroopEventList.AddEvent` 中没有订阅 `OnApplyTroopEvent`
- 或者订阅被意外清除

**修复：**
```csharp
// TroopEventList.cs
public void AddEvent(TroopEvent te, bool add = true)
{
    // ...
    
    // 🔥 关键：订阅事件
    te.OnApplyTroopEvent += te_OnApplyTroopEvent;
}

void te_OnApplyTroopEvent(TroopEvent te, Troop troop)
{
    Session.MainGame.mainGameScreen.TroopApplyTroopEvent(te, troop);
}
```

### 问题 2：ApplyTroopEvents 未被调用

**症状：**
- 对话显示正常
- 但对话关闭后，影响没有施加
- `TroopEventsToApply` 队列一直有数据，但从未清空

**诊断：**
```csharp
// 在 MainGameScreen.TroopApplyTroopEvent 中添加日志
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[TroopApplyTroopEvent] 设置关闭回调: ApplyTroopEvents");
#endif

// 在 GameScenario.ApplyTroopEvents 中添加日志
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[ApplyTroopEvents] 开始施加事件影响，队列大小={this.TroopEventsToApply.Count}");
#endif
```

**原因：**
- `tupianwenziPlugin.SetCloseFunction` 没有正确设置回调
- 或者对话关闭时回调没有被触发

**修复：**
```csharp
// 检查 tupianwenziPlugin 的实现
// 确保 CloseFunction 在对话关闭时被调用
```

### 问题 3：ApplyEventEffects 条件不满足

**症状：**
- `ApplyTroopEvents()` 被调用
- 但 `ApplyEventEffects()` 中的代码没有执行

**诊断：**
```csharp
// 在 TroopEvent.ApplyEventEffects 中添加日志
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[ApplyEventEffects] self={self?.DisplayName}, Destroyed={self?.Destroyed}, Happened={this.Happened}, Repeatable={this.Repeatable}");
#endif
```

**原因：**
- `self == null`：部队已被销毁
- `self.Destroyed == true`：部队已被销毁
- `this.Happened == true && this.Repeatable == false`：事件已发生且不可重复

**修复：**
- 检查事件的 `Repeatable` 属性是否正确设置
- 检查部队是否在事件触发后被销毁

### 问题 4：Effect 数据为空

**症状：**
- `ApplyEventEffects()` 被调用
- 但 `SelfEffects.Count == 0`，没有影响可施加

**诊断：**
```csharp
// 在 TroopEvent.ApplyEventEffects 中添加日志
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[ApplyEventEffects] SelfEffects.Count={this.SelfEffects.Count}, EffectPersons.Count={this.EffectPersons.Count}, EffectAreas.Count={this.EffectAreas.Count}");
#endif
```

**原因：**
- 事件数据加载失败
- `SelfEffects`、`EffectPersons`、`EffectAreas` 集合为空

**修复：**
- 检查事件数据文件（JSON）
- 检查反序列化逻辑

---

## 🔧 诊断步骤

### 步骤 1：添加完整的日志链

```csharp
// 1. Troop.cs（事件触发）
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[Troop] 触发事件: {event2.Name}, 部队: {this.DisplayName}");
#endif

// 2. TroopEvent.ApplyEventDialogs（对话显示）
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[TroopEvent.ApplyEventDialogs] 事件: {this.Name}, OnApplyTroopEvent订阅数: {this.OnApplyTroopEvent?.GetInvocationList().Length ?? 0}");
#endif

// 3. MainGameScreen.TroopApplyTroopEvent（UI 显示）
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[MainGameScreen.TroopApplyTroopEvent] 事件: {te.Name}, 对话数: {te.Dialogs.Count}");
#endif

// 4. GameScenario.ApplyTroopEvents（影响施加）
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[GameScenario.ApplyTroopEvents] 队列大小: {this.TroopEventsToApply.Count}");
#endif

// 5. TroopEvent.ApplyEventEffects（实际施加）
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[TroopEvent.ApplyEventEffects] 事件: {this.Name}, SelfEffects: {this.SelfEffects.Count}, EffectPersons: {this.EffectPersons.Count}");
#endif
```

### 步骤 2：运行游戏并触发事件

1. 启动游戏
2. 移动部队触发事件
3. 查看 Debug 输出

### 步骤 3：分析日志输出

**正常流程：**
```
[Troop] 触发事件: 遇到山贼, 部队: 曹操军
[TroopEvent.ApplyEventDialogs] 事件: 遇到山贼, OnApplyTroopEvent订阅数: 1
[MainGameScreen.TroopApplyTroopEvent] 事件: 遇到山贼, 对话数: 2
[GameScenario.ApplyTroopEvents] 队列大小: 1
[TroopEvent.ApplyEventEffects] 事件: 遇到山贼, SelfEffects: 1, EffectPersons: 0
```

**异常流程示例 1（OnApplyTroopEvent 未订阅）：**
```
[Troop] 触发事件: 遇到山贼, 部队: 曹操军
[TroopEvent.ApplyEventDialogs] 事件: 遇到山贼, OnApplyTroopEvent订阅数: 0  ← 问题！
[TroopEvent.ApplyEventDialogs] ⚠️ OnApplyTroopEvent 为 null！事件未订阅！
（没有后续日志）
```

**异常流程示例 2（ApplyTroopEvents 未调用）：**
```
[Troop] 触发事件: 遇到山贼, 部队: 曹操军
[TroopEvent.ApplyEventDialogs] 事件: 遇到山贼, OnApplyTroopEvent订阅数: 1
[MainGameScreen.TroopApplyTroopEvent] 事件: 遇到山贼, 对话数: 2
（没有 ApplyTroopEvents 日志）  ← 问题！
```

**异常流程示例 3（Effect 数据为空）：**
```
[Troop] 触发事件: 遇到山贼, 部队: 曹操军
[TroopEvent.ApplyEventDialogs] 事件: 遇到山贼, OnApplyTroopEvent订阅数: 1
[MainGameScreen.TroopApplyTroopEvent] 事件: 遇到山贼, 对话数: 2
[GameScenario.ApplyTroopEvents] 队列大小: 1
[TroopEvent.ApplyEventEffects] 事件: 遇到山贼, SelfEffects: 0, EffectPersons: 0  ← 问题！
```

---

## 📝 修复方案

### 🔥 根本修复：LoadDataPhase.cs 使用正确的方法

**位置：** `WorldOfTheThreeKingdoms/Serialization/Phases/LoadDataPhase.cs` 第 1688 行

**当前代码（错误）：**
```csharp
scenario.TroopEvents.Add(evt);  // ❌ 不会订阅事件
```

**修复代码：**
```csharp
scenario.TroopEvents.AddTroopEventWithEvent(evt, true);  // ✅ 正确订阅事件
```

**完整修复：**
```csharp
// LoadDataPhase.cs（约第 1650-1690 行）
private static int LoadTroopEvents(GameScenario scenario, ScenarioDTO dto)
{
    int count = 0;
    
    if (dto.TroopEvents == null || dto.TroopEvents.Count == 0)
    {
        #if DEBUG
        System.Diagnostics.Debug.WriteLine("[LoadDataPhase] ⚠️ 剧本没有部队事件数据");
        #endif
        return count;
    }
    
    foreach (var eventDto in dto.TroopEvents)
    {
        var evt = new TroopEvent
        {
            ID = eventDto.ID,
            Name = eventDto.Name ?? "",
            // ... 其他属性 ...
        };
        
        // 🔥 关键修复：使用 AddTroopEventWithEvent 而不是 Add
        // 原因：AddTroopEventWithEvent 会订阅 OnApplyTroopEvent 事件
        // 日期：2026-03-23
        scenario.TroopEvents.AddTroopEventWithEvent(evt, true);
        
        count++;
        
        #if DEBUG
        if (count % 10 == 0)
        {
            System.Diagnostics.Debug.WriteLine($"[LoadDataPhase] 已加载 {count} 个部队事件");
        }
        #endif
    }
    
    #if DEBUG
    System.Diagnostics.Debug.WriteLine($"[LoadDataPhase] ✅ 部队事件加载完成，共 {count} 个");
    #endif
    
    return count;
}
```

---

### 方案 1：确保 OnApplyTroopEvent 正确订阅（备用方案）

```csharp
// TroopEventList.cs
public void AddEvent(TroopEvent te, bool add = true)
{
    if (add)
    {
        base.AddGameObject(te);
    }
    
    // 🔥 关键修复：确保订阅事件
    te.OnApplyTroopEvent -= te_OnApplyTroopEvent;  // 先取消订阅（避免重复）
    te.OnApplyTroopEvent += te_OnApplyTroopEvent;  // 重新订阅
}

void te_OnApplyTroopEvent(TroopEvent te, Troop troop)
{
    if (Session.MainGame?.mainGameScreen != null)
    {
        Session.MainGame.mainGameScreen.TroopApplyTroopEvent(te, troop);
    }
    else
    {
        #if DEBUG
        System.Diagnostics.Debug.WriteLine($"⚠️ Session.MainGame.mainGameScreen 为 null！");
        #endif
    }
}
```

### 方案 2：确保 ApplyTroopEvents 被调用

```csharp
// MainGameScreen.cs
public override void TroopApplyTroopEvent(TroopEvent te, Troop troop)
{
    #if DEBUG
    System.Diagnostics.Debug.WriteLine($"[TroopApplyTroopEvent] 事件: {te.Name}, 对话数: {te.Dialogs.Count}");
    #endif
    
    if ((((Session.Current.Scenario.CurrentPlayer == null) || Session.Current.Scenario.CurrentPlayer.IsPositionKnown(troop.Position)) || Session.GlobalVariables.SkyEye) && (te.Dialogs.Count > 0))
    {
        // 显示对话...
        
        if (Setting.Current.GlobalVariables.DialogShowTime > 0)
        {
            // 🔥 关键修复：确保回调被设置
            this.Plugins.tupianwenziPlugin.SetCloseFunction(new GameDelegates.VoidFunction(() => {
                #if DEBUG
                System.Diagnostics.Debug.WriteLine($"[TroopApplyTroopEvent] 对话关闭，调用 ApplyTroopEvents");
                #endif
                Session.Current.Scenario.ApplyTroopEvents();
            }));
            this.Plugins.tupianwenziPlugin.IsShowing = true;
        }
        else
        {
            #if DEBUG
            System.Diagnostics.Debug.WriteLine($"[TroopApplyTroopEvent] 无对话延迟，立即调用 ApplyTroopEvents");
            #endif
            Session.Current.Scenario.ApplyTroopEvents();
        }
    }
    else
    {
        #if DEBUG
        System.Diagnostics.Debug.WriteLine($"[TroopApplyTroopEvent] 无对话或不可见，立即调用 ApplyTroopEvents");
        #endif
        Session.Current.Scenario.ApplyTroopEvents();
    }
}
```

### 方案 3：检查事件数据加载

```csharp
// 在数据加载后验证
#if DEBUG
foreach (GameObject obj in scenario.TroopEvents)
{
    TroopEvent te = obj as TroopEvent;
    if (te != null)
    {
        System.Diagnostics.Debug.WriteLine($"[TroopEvent] ID={te.ID}, Name={te.Name}, SelfEffects={te.SelfEffects.Count}, EffectPersons={te.EffectPersons.Count}, EffectAreas={te.EffectAreas.Count}");
    }
}
#endif
```

---

## 🎯 根本原因确认

通过代码分析，我发现了问题的根本原因：

### 问题：数据加载时使用了错误的方法

**发现：**

在 `LoadDataPhase.cs` 第 1688 行：
```csharp
scenario.TroopEvents.Add(evt);  // ❌ 错误：使用了 Add 方法
```

在 `GameScenario.cs` 第 5903 行（旧数据加载）：
```csharp
this.TroopEvents.AddTroopEventWithEvent(te, false);  // ✅ 正确：使用了 AddTroopEventWithEvent
```

**问题分析：**

1. `TroopEventList.Add(GameObject obj)` 方法：
   - 只是将对象添加到列表
   - **不会订阅 `OnApplyTroopEvent` 事件**
   - 结果：事件触发时，`OnApplyTroopEvent` 为 null

2. `TroopEventList.AddTroopEventWithEvent(TroopEvent te, bool add)` 方法：
   - 将对象添加到列表
   - **会订阅 `OnApplyTroopEvent` 事件**（第 46 行）
   - 结果：事件触发时，`OnApplyTroopEvent` 正常调用

**影响：**

- 新的 JSON 数据加载（`LoadDataPhase.cs`）使用了 `Add` 方法 → 事件未订阅 → 影响不施加
- 旧的数据加载（`GameScenario.cs`）使用了 `AddTroopEventWithEvent` 方法 → 事件正常订阅 → 影响正常施加

**结论：**

这是一个数据加载阶段的 BUG，新的序列化系统（`LoadDataPhase.cs`）在加载 TroopEvent 时使用了错误的方法。

---

## ✅ 修复验证

### 验证步骤

1. **编译游戏**
   ```bash
   dotnet build
   ```

2. **启动游戏并触发事件**
   - 移动部队到事件触发点
   - 观察事件对话是否显示
   - 观察事件影响是否正确施加

3. **检查 Debug 日志**
   ```
   [LoadTroopEvents] 已添加TroopEvent 遇到山贼(ID:1)
   [Troop] 触发事件: 遇到山贼, 部队: 曹操军
   [TroopEvent.ApplyEventDialogs] 事件: 遇到山贼, OnApplyTroopEvent订阅数: 1  ← 应该是 1，不是 0
   [MainGameScreen.TroopApplyTroopEvent] 事件: 遇到山贼, 对话数: 2
   [GameScenario.ApplyTroopEvents] 队列大小: 1
   [TroopEvent.ApplyEventEffects] 事件: 遇到山贼, SelfEffects: 1, EffectPersons: 0
   ```

4. **验证影响施加**
   - 检查部队士气、粮草等属性是否按事件定义变化
   - 检查武将属性是否按事件定义变化

### 预期结果

- ✅ 事件对话正常显示
- ✅ 事件影响正确施加
- ✅ `OnApplyTroopEvent` 订阅数为 1（不是 0）
- ✅ `ApplyEventEffects` 正常执行

---

## 📚 相关文件

- `WorldOfTheThreeKingdoms/GameObjects/TroopEvent.cs`（事件定义）
- `WorldOfTheThreeKingdoms/GameObjects/TroopEventList.cs`（事件列表）
- `WorldOfTheThreeKingdoms/GameObjects/Troop.cs`（事件触发）
- `WorldOfTheThreeKingdoms/GameObjects/GameScenario.cs`（影响施加）
- `WorldOfTheThreeKingdoms/GameScreens/MainGameScreen.cs`（UI 显示）

---

**维护者：** Lead Architect  
**最后更新：** 2026-03-23
