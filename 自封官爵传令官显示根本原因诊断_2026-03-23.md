# 自封官爵传令官显示问题：根本原因诊断（2026-03-23）

日期：2026-03-23  
状态：**已找到根本原因并修复**

---

## 🔥 根本原因

### 问题 1：第二个 XML 文件中的 `Zili` 分支使用了错误的属性名

**位置：** `Content/Textures/GameComponents/tupianwenzi/Data/tupianwenziTextTree.xml`

**错误代码（第 547-551 行）：**
```xml
<Branch Name="Zili">
  <Leaf Text="承蒙各位爱戴，今日我自封为" Color="4294967295"></Leaf>
  <Leaf Property="TextDestinationString" Color="4278255360"></Leaf>  <!-- ❌ 错误属性 -->
  <Leaf Text="。我们齐心协力，共创大业！" Color="4294967295"></Leaf>
</Branch>
```

**正确代码：**
```xml
<Branch Name="Zili">
  <Leaf Text="承蒙各位爱戴，今日孤自封为" Color="4294967295"></Leaf>
  <Leaf Property="TextResultString" Color="4278255360"></Leaf>  <!-- ✅ 正确属性 -->
  <Leaf Text="。我们齐心协力，共创大业！" Color="4294967295"></Leaf>
</Branch>
```

**影响：**
- `SelfAdvancement()` 方法传入的是 `gj2.Name`（官爵名称）作为 `TextResultString`
- 但 XML 使用了 `TextDestinationString`，导致显示空白或错误内容
- 可能导致 UI 回退到默认的 `shengguan` 分支（传令官口吻）

---

## 📋 完整的代码与 XML 映射关系

### Advancement() 方法（朝廷加封）

**代码调用：**
```csharp
Session.MainGame.mainGameScreen.xianshishijiantupian(
    this.Leader,           // Person p → 君主本人
    gj.Name,              // TextResultString → 官爵名称
    "LeaderAdvancement",  // 分支名
    "shengguan.jpg",
    "",
    true);
```

**XML 分支（两个文件都正确）：**
```xml
<Branch Name="LeaderAdvancement">
  <Leaf Text="承蒙朝廷恩准，今日孤受封为" Color="4294967295"></Leaf>
  <Leaf Property="TextResultString" Color="4278255360"></Leaf>  <!-- 显示官爵名称 -->
  <Leaf Text="。望诸君与我同心，共济天下！" Color="4294967295"></Leaf>
</Branch>
```

### SelfAdvancement() 方法（自封官爵）

**代码调用：**
```csharp
Session.MainGame.mainGameScreen.xianshishijiantupian(
    this.Leader,      // Person p → 君主本人
    gj2.Name,        // TextResultString → 官爵名称
    "Zili",          // 分支名
    "shengguan.jpg",
    "",
    true);
```

**XML 分支：**

✅ **第一个文件正确**（`Content/Data/Plugins/tupianwenziTextTree.xml`）：
```xml
<Branch Name="Zili">
  <Leaf Text="承蒙各位爱戴，今日孤自封为" Color="4294967295"></Leaf>
  <Leaf Property="TextResultString" Color="4278255360"></Leaf>  <!-- ✅ 正确 -->
  <Leaf Text="。我们齐心协力，共创大业！" Color="4294967295"></Leaf>
</Branch>
```

❌ **第二个文件错误**（`Content/Textures/GameComponents/tupianwenzi/Data/tupianwenziTextTree.xml`）：
```xml
<Branch Name="Zili">
  <Leaf Text="承蒙各位爱戴，今日我自封为" Color="4294967295"></Leaf>
  <Leaf Property="TextDestinationString" Color="4278255360"></Leaf>  <!-- ❌ 错误 -->
  <Leaf Text="。我们齐心协力，共创大业！" Color="4294967295"></Leaf>
</Branch>
```

---

## 🔧 修复内容

### 修复 1：统一 Zili 分支的属性名

**文件：** `Content/Textures/GameComponents/tupianwenzi/Data/tupianwenziTextTree.xml`

**修改：**
- 将 `TextDestinationString` 改为 `TextResultString`
- 统一文案："今日我自封为" → "今日孤自封为"（与第一个文件一致）

---

## 🎯 为什么之前的修复无效

### 用户反馈："重启游戏后依然是传令官对话框"

**原因分析：**

1. **两个 XML 文件不一致**：
   - 第一个文件（`Content/Data/Plugins/tupianwenziTextTree.xml`）已正确修复
   - 第二个文件（`Content/Textures/GameComponents/tupianwenzi/Data/tupianwenziTextTree.xml`）仍有错误
   - 游戏运行时可能加载了第二个文件

2. **属性名错误导致回退**：
   - XML 使用 `TextDestinationString`，但代码传入的是 `TextResultString`
   - 当属性不匹配时，UI 可能显示空白或回退到默认分支
   - 默认分支可能是旧的 `shengguan`（传令官口吻）

3. **晋升分流逻辑**：
   - 有皇帝 + 非异族 + 达标 → `Advancement()`（朝廷加封）
   - 其他情况 → `SelfAdvancement()`（自封）
   - 玩家可能走的是 `SelfAdvancement()` 路径，而这个路径的 XML 有错误

---

## 📊 验证步骤

### 步骤 1：确认运行路径

用户需要检查：
1. 任务管理器 → 右键 `WorldOfTheThreeKingdoms.exe` → "打开文件所在位置"
2. 确认运行的是构建输出目录

### 步骤 2：确认 XML 文件

检查两个 XML 文件：
1. `Content\Data\Plugins\tupianwenziTextTree.xml`
2. `Content\Textures\GameComponents\tupianwenzi\Data\tupianwenziTextTree.xml`

确认两个文件中的 `Zili` 分支都使用 `TextResultString`。

### 步骤 3：重新构建

```bash
dotnet build --configuration Release
```

### 步骤 4：游戏内测试

1. 新开剧本或读档
2. 等待势力晋升事件触发
3. 检查弹窗是否显示君主本人和正确文案

---

## 🚨 关键教训

### 教训 1：项目中有多个 XML 文件副本

**问题：**
- `Content/Data/Plugins/tupianwenziTextTree.xml`
- `Content/Textures/GameComponents/tupianwenzi/Data/tupianwenziTextTree.xml`

两个文件内容应该一致，但实际上有差异。

**解决方案：**
- 修改时必须同时修改两个文件
- 或者使用符号链接/文件引用避免重复

### 教训 2：属性名必须精确匹配

**问题：**
- 代码传入 `TextResultString`
- XML 使用 `TextDestinationString`
- 属性名不匹配导致显示错误

**解决方案：**
- 修改 XML 时仔细检查属性名
- 使用 DEBUG 日志验证传入的参数

### 教训 3：必须检查所有相关文件

**问题：**
- 只修改了第一个 XML 文件
- 忽略了第二个 XML 文件
- 导致修复无效

**解决方案：**
- 使用 `grep` 搜索所有相关文件
- 确认所有副本都已修改

---

## 📝 相关文档

- [复用现有功能规范](.codex/复用现有功能规范.md)
- [数据加载问题诊断规范](.codex/数据加载问题诊断规范.md)
- [ANTI-BAND-AID 协议](.agent/rules/global-rules.md)

---

**最后更新：** 2026-03-23  
**维护者：** Lead Architect  
**状态：** 已修复，等待用户验证
