# 军师叛乱预警事件判定逻辑分析

**日期：** 2026-03-12  
**分析对象：** 军师叛乱预警事件系统  
**状态：** 已实现，仅在调试日志中可见

---

## 📋 系统概览

军师叛乱预警事件是一个**被动触发**的智能建议系统，在回合开始时自动扫描势力内的风险，当检测到武将忠诚度极低时触发紧急警报。

### 核心组件

```
TurnManager (回合管理器)
    ↓ 回合开始时调用
StrategistManager.CheckCriticalRisks() (军师系统)
    ↓ 检测到危险时触发
EventManager.TriggerStrategistEvent() (事件管理器)
    ↓ 广播事件
StrategistUI (军师UI) - 订阅者
    ↓ 显示弹窗
玩家看到警报
```

---

## 🔍 判定逻辑详解

### 1. 触发时机

**位置：** `TurnManager.OnTurnStart()`  
**文件：** `WorldOfTheThreeKingdoms/GameManager/TurnManager.cs`

```csharp
public void OnTurnStart(Faction playerFaction)
{
    // ... 其他逻辑 ...
    
    // 2. 让军师扫描风险
    Person strategist = GetStrategist(playerFaction);
    if (strategist != null)
    {
        System.Diagnostics.Debug.WriteLine($"回合开始 - 军师 {strategist.Name} 开始风险扫描");
        StrategistManager.CheckCriticalRisks(playerFaction);  // 🔥 关键调用
    }
}
```

**触发条件：**
- ✅ 回合开始时自动触发
- ✅ 势力必须有军师（智力 ≥ 80）
- ✅ 势力未被消灭

---

### 2. 叛乱预警判定条件

**位置：** `StrategistManager.CheckCriticalRisks()`  
**文件：** `WorldOfTheThreeKingdoms/GameManager/StrategistSystem.cs`

```csharp
public static void CheckCriticalRisks(Faction faction)
{
    // 1. 检查忠诚度危机
    foreach (Person officer in faction.Persons.GetList())
    {
        if (officer.Loyalty < 40)  // 🔥 关键判定：忠诚度 < 40
        {
            // 组装数据 - 使用配置化对话
            string content = AdvisorDialogueManager.GetGenericDialogue(
                faction, 
                strategist, 
                "Emergency_Rebellion",  // 🔥 对话模板Key
                new Dictionary<string, string> { 
                    { "{Person}", officer.Name } 
                }
            );
            
            var data = new AdviceData
            {
                Level = RiskLevel.Critical,
                Title = "【叛乱预警！】",
                Content = $"军师【{strategistName}】：{content}",
                ButtonText = "立即赏赐",
                Type = AdviceType.Emergency,
                OnClick = () => { /* 打开赏赐界面的代码 */ }
            };
            
            // 🔥 触发事件
            EventManager.TriggerStrategistEvent(data);
            return; // 每次只报一个最紧急的，避免弹窗重叠
        }
    }
}
```

**判定条件：**
- 🎯 **核心条件：** `officer.Loyalty < 40`
- 🎯 **优先级：** 只报告第一个检测到的低忠诚武将
- 🎯 **风险等级：** `RiskLevel.Critical`（紧急军情）

---

### 3. 对话模板系统

**位置：** `AdvisorDialogueManager.AddDefaults()`  
**文件：** `WorldOfTheThreeKingdoms/GameManager/AdvisorDialogueManager.cs`

```csharp
// 紧急警报对话
templates["Emergency_Rebellion"] = 
    "{LeaderAddress}！{SelfAddress}夜观天象，发现 {Person} 面露反骨，恐近日即将叛变！";
```

**占位符替换：**
- `{LeaderAddress}` → 称呼（如"主公"、"陛下"）
- `{SelfAddress}` → 自称（如"臣"、"在下"）
- `{Person}` → 低忠诚武将的名字

**最终效果：**
```
军师【荀攸】：主公！臣夜观天象，发现 袁术 面露反骨，恐近日即将叛变！
```

---

### 4. 事件广播机制

**位置：** `EventManager.TriggerStrategistEvent()`  
**文件：** `WorldOfTheThreeKingdoms/GameManager/EventManager.cs`

```csharp
public static event Action<AdviceData> OnStrategistInterrupt;

public static void TriggerStrategistEvent(AdviceData data)
{
    System.Diagnostics.Debug.WriteLine($"[EventManager] 🔥 触发军师事件: {data.Title}");
    System.Diagnostics.Debug.WriteLine($"[EventManager] 📊 当前订阅者数量: {OnStrategistInterrupt?.GetInvocationList().Length ?? 0}");
    
    OnStrategistInterrupt?.Invoke(data);  // 🔥 广播事件
}
```

**工作原理：**
- 使用 C# 事件机制（观察者模式）
- 任何订阅了 `OnStrategistInterrupt` 的组件都会收到通知
- 目前只有 `StrategistUI` 订阅了这个事件

---

## 🐛 根本问题：UI 功能被禁用了！

### 问题分析

根据日志输出：
```
[EventManager] 🔥 触发军师事件: 【叛乱预警！】
[EventManager] 📊 当前订阅者数量: 1
[StrategistUI] 收到军师建议: 军师【荀攸】：主公！臣夜观天象，发现 袁术 面露反骨，恐近日即将叛变！
```

**结论：**
- ✅ 事件系统正常工作
- ✅ StrategistUI 正确接收到事件
- ❌ UI 没有显示弹窗

### 🔥 根本原因：代码被注释掉了！

查看 `StrategistUI.cs` 的代码，发现关键问题：

```csharp
public void Update(GameTime gameTime)
{
    // 禁用独立的军师按钮功能，改用建筑菜单
    return;  // 🔥 直接返回，后面的逻辑都不执行了！
    
    // ... 后面的代码永远不会执行 ...
}

public void Draw(SpriteBatch spriteBatch)
{
    // 禁用独立的军师按钮绘制，改用建筑菜单
    return;  // 🔥 直接返回，UI 永远不会绘制！
    
    // ... 后面的代码永远不会执行 ...
}
```

**事件处理逻辑：**

```csharp
private void OnStrategistAdviceReceived(AdviceData data)
{
    System.Diagnostics.Debug.WriteLine($"[StrategistUI] 收到军师建议: {data.Content}");
    
    // 更新显示内容
    currentAdvice = data.Content;
    
    // TODO: 这里可以添加更多UI响应逻辑
    // 例如：显示弹窗、播放音效等
    // 🔥 问题：只是更新了文本，但没有显示弹窗！
}
```

### 三重问题

1. **Update() 被禁用**
   - 第一行就 `return`，所有交互逻辑都不执行

2. **Draw() 被禁用**
   - 第一行就 `return`，UI 永远不会显示

3. **事件处理不完整**
   - `OnStrategistAdviceReceived()` 只更新了文本
   - 没有显示弹窗的逻辑
   - 即使有弹窗逻辑，Draw() 也被禁用了

---

## 🔧 修复方案

### 方案1：启用 StrategistUI 的弹窗功能（推荐）

移除 `Update()` 和 `Draw()` 中的 `return` 语句，并完善弹窗逻辑：

```csharp
private AdviceData currentAdviceData = null;
private bool showPopup = false;

private void OnStrategistAdviceReceived(AdviceData data)
{
    System.Diagnostics.Debug.WriteLine($"[StrategistUI] 收到军师建议: {data.Content}");
    
    // 保存数据并显示弹窗
    currentAdviceData = data;
    showPopup = true;
    currentAdvice = data.Content;
}

public void Update(GameTime gameTime)
{
    // 移除这行：return;
    
    // 处理弹窗交互
    if (showPopup && currentAdviceData != null)
    {
        // 检测按钮点击
        MouseState mouse = Mouse.GetState();
        // ... 弹窗交互逻辑 ...
    }
    
    // ... 原有的其他逻辑 ...
}

public void Draw(SpriteBatch spriteBatch)
{
    // 移除这行：return;
    
    // 绘制弹窗
    if (showPopup && currentAdviceData != null)
    {
        DrawAdvicePopup(spriteBatch, currentAdviceData);
    }
    
    // ... 原有的其他绘制逻辑 ...
}
```

### 方案2：使用 MainGameScreen 的弹窗系统（更简单）

直接在事件处理中调用 MainGameScreen 的弹窗：

```csharp
private void OnStrategistAdviceReceived(AdviceData data)
{
    System.Diagnostics.Debug.WriteLine($"[StrategistUI] 收到军师建议: {data.Content}");
    
    // 使用 MainGameScreen 的弹窗系统
    var mainGameScreen = Session.MainGame?.mainGameScreen;
    if (mainGameScreen != null)
    {
        // 显示确认对话框
        mainGameScreen.ShowConfirmationDialog(
            data.Title,
            data.Content,
            data.ButtonText,
            () => {
                // 按钮点击回调
                data.OnClick?.Invoke();
            }
        );
    }
}
```

### 方案3：集成到建筑菜单系统

既然注释说"改用建筑菜单"，可以在建筑菜单中添加军师建议的显示：

```csharp
// 在 AdvisorContextMenuHandler 或相关菜单系统中
private void OnStrategistAdviceReceived(AdviceData data)
{
    // 在建筑菜单中显示军师建议
    ShowAdvisorAdviceInMenu(data);
}
```

---

## 📊 完整的数据流

```
回合开始
    ↓
TurnManager.OnTurnStart()
    ↓
检查是否有军师（智力 ≥ 80）
    ↓ 有军师
StrategistManager.CheckCriticalRisks()
    ↓
遍历势力所有武将
    ↓
发现 officer.Loyalty < 40
    ↓
AdvisorDialogueManager.GetGenericDialogue()
    ↓ 生成对话文本
创建 AdviceData 数据包
    - Level: Critical
    - Title: "【叛乱预警！】"
    - Content: "军师【荀攸】：主公！臣夜观天象，发现 袁术 面露反骨，恐近日即将叛变！"
    - ButtonText: "立即赏赐"
    ↓
EventManager.TriggerStrategistEvent(data)
    ↓ 广播事件
OnStrategistInterrupt?.Invoke(data)
    ↓
StrategistUI.OnStrategistAdviceReceived(data)
    ↓ 📝 输出调试日志
System.Diagnostics.Debug.WriteLine($"[StrategistUI] 收到军师建议: {data.Content}")
    ↓ ❓ 应该显示弹窗，但没有
??? ShowAdvicePopup(data) ???
```

---

## 🎯 判定参数总结

| 参数 | 值 | 说明 |
|------|-----|------|
| **触发时机** | 回合开始 | `TurnManager.OnTurnStart()` |
| **军师要求** | 智力 ≥ 80 | 只有高智力武将才能当军师 |
| **忠诚度阈值** | < 40 | 低于此值触发叛乱预警 |
| **风险等级** | Critical | 紧急军情，应该强制打断 |
| **优先级** | 第一个 | 只报告第一个检测到的低忠诚武将 |
| **对话模板** | Emergency_Rebellion | 可配置的对话文本 |
| **按钮文字** | "立即赏赐" | 建议玩家的操作 |

---

## 🔍 下一步调查方向

1. **查看 StrategistUI 的完整实现**
   - 事件订阅代码
   - 弹窗显示逻辑
   - Update() 和 Draw() 方法

2. **查看 MainGameScreen 的初始化**
   - StrategistUI 是否被正确添加？
   - 是否在正确的时机调用 Update() 和 Draw()？

3. **查看弹窗系统**
   - 是否有统一的弹窗管理器？
   - 弹窗的显示逻辑是否完整？

---

## 💡 设计亮点

1. **事件驱动架构**
   - 使用观察者模式，解耦业务逻辑和UI
   - 易于扩展，可以添加更多订阅者

2. **配置化对话系统**
   - 对话文本可以通过XML配置
   - 支持占位符替换，灵活性高

3. **优先级机制**
   - 只报告最紧急的一个问题
   - 避免弹窗重叠，提升用户体验

4. **Anti-Band-Aid 原则**
   - 不做过度的空检查
   - 让数据错误暴露，便于调试

---

**最后更新：** 2026-03-12  
**维护者：** Lead Architect
