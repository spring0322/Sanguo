# 自封官爵传令官显示问题：最终诊断（2026-03-23）

日期：2026-03-23  
状态：**需要添加 DEBUG 日志追踪**

---

## 🔥 用户反馈的关键信息

> "我查所有势力，这个事件发生后，没有哪个势力的爵位是王，所以很可能事件系统出了问题，导致乱报"

**这说明：**
1. 弹窗显示的内容（"孤自封魏王"）与实际游戏状态不符
2. 没有任何势力的爵位达到"王"
3. 可能是错误的事件被触发，或者事件数据错误

---

## 📋 已排查的内容

### 1. 代码修改正确

✅ `Advancement()` 方法：
- 使用 `this.Leader` 作为发言人
- 使用 `LeaderAdvancement` 分支
- 有 DEBUG 日志

✅ `SelfAdvancement()` 方法：
- 使用 `this.Leader` 作为发言人
- 使用 `Zili` 分支
- 有 DEBUG 日志

✅ XML 文件：
- 两个 XML 文件都已修复
- `Zili` 分支使用正确的 `TextResultString`

### 2. 调用链正确

```
shizheshengguan()
  → Advancement() 或 SelfAdvancement()
    → xianshishijiantupian(this.Leader, gj.Name, "LeaderAdvancement"/"Zili", ...)
      → tupianwenziPlugin.SetGameObjectBranch(p, p, ...)
        → tupianwenzi.SetGameObjectBranch(gongfang, gameObject, ...)
          → GameObjectAndBranchName(gongfang, ...)
            → this.person = (p is Person ? (Person)p : null)
```

### 3. 事件系统正常

✅ 事件订阅中没有显示弹窗的代码
✅ 只有 `Advancement()` 和 `SelfAdvancement()` 两个地方调用晋升弹窗

---

## 🚨 可能的根本原因

### 假设 1：弹窗队列混乱

**问题：** `tupianwenzi` 使用队列 (`DisplayQueue`) 存储待显示的弹窗，可能有旧的弹窗残留。

**证据：**
- 用户说"没有哪个势力的爵位是王"
- 但弹窗显示"孤自封魏王"
- 说明弹窗内容与当前游戏状态不符

**可能性：**
- 之前的游戏会话中有势力晋升到"王"
- 弹窗被加入队列但没有显示
- 重新开始游戏后，旧弹窗仍在队列中
- 新的晋升事件触发时，显示的是旧弹窗

### 假设 2：NeutralPerson 被错误使用

**问题：** `Scenario.NeutralPerson`（传令官，ID=7007）在某些情况下被用作默认发言人。

**证据：**
- 截图显示标题是"传令官"
- `GameObjectAndBranchName` 构造函数：`this.person = (p is Person ? (Person)p : null)`
- 如果传入的不是 `Person` 类型，会被设置为 `null`

**可能性：**
- 某个地方传入了非 `Person` 对象（如 `Faction`）
- `GameObjectAndBranchName.person` 被设置为 `null`
- UI 回退到使用 `NeutralPerson` 作为默认发言人

### 假设 3：多个势力同时晋升

**问题：** 多个势力在同一回合晋升，弹窗显示混乱。

**可能性：**
- AI 势力 A 晋升，弹窗加入队列
- AI 势力 B 晋升，弹窗加入队列
- 玩家势力晋升，弹窗加入队列
- 显示时顺序错乱或内容混淆

---

## 🔧 诊断方案：添加 DEBUG 日志

### 步骤 1：在 `Advancement()` 和 `SelfAdvancement()` 中添加详细日志

```csharp
#if DEBUG
System.Diagnostics.Debug.WriteLine($"========== 晋升事件开始 ==========");
System.Diagnostics.Debug.WriteLine($"[Advancement] 势力={this.Name}(ID:{this.ID})");
System.Diagnostics.Debug.WriteLine($"[Advancement] 君主={this.Leader.Name}(ID:{this.Leader.ID})");
System.Diagnostics.Debug.WriteLine($"[Advancement] 当前爵位={this.guanjue}, 新爵位={gj.Name}");
System.Diagnostics.Debug.WriteLine($"[Advancement] 是否玩家势力={shouldShowCurrentPlayerDialog}");
System.Diagnostics.Debug.WriteLine($"========== 晋升事件结束 ==========");
#endif
```

### 步骤 2：在 `tupianwenzi.SetGameObjectBranch` 中添加日志

```csharp
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[SetGameObjectBranch] gongfang={gongfang?.GetType().Name}, Name={(gongfang as Person)?.Name ?? "非Person"}");
System.Diagnostics.Debug.WriteLine($"[SetGameObjectBranch] gameObject={gameObject?.GetType().Name}");
System.Diagnostics.Debug.WriteLine($"[SetGameObjectBranch] branchName={branchName}");
#endif
```

### 步骤 3：在 `GameObjectAndBranchName` 构造函数中添加日志

```csharp
#if DEBUG
System.Diagnostics.Debug.WriteLine($"[GameObjectAndBranchName] 传入对象类型={p?.GetType().Name}");
System.Diagnostics.Debug.WriteLine($"[GameObjectAndBranchName] 是否Person={p is Person}");
System.Diagnostics.Debug.WriteLine($"[GameObjectAndBranchName] person.Name={this.person?.Name ?? "null"}");
System.Diagnostics.Debug.WriteLine($"[GameObjectAndBranchName] branchName={name}");
#endif
```

### 步骤 4：在 `tupianwenzi.DequeueAndDisplay` 中添加日志

```csharp
#if DEBUG
System.Diagnostics.Debug.WriteLine($"========== 显示弹窗 ==========");
System.Diagnostics.Debug.WriteLine($"[DequeueAndDisplay] 队列长度={DisplayQueue.Count}");
System.Diagnostics.Debug.WriteLine($"[DequeueAndDisplay] 说话人={name.person?.Name ?? "null"}(ID:{name.person?.ID ?? -1})");
System.Diagnostics.Debug.WriteLine($"[DequeueAndDisplay] 分支={name.branchName}");
System.Diagnostics.Debug.WriteLine($"[DequeueAndDisplay] 文本={string.Join("", name.texts.Select(t => t.Text))}");
System.Diagnostics.Debug.WriteLine($"========== 显示弹窗结束 ==========");
#endif
```

---

## 📝 用户操作步骤

1. **重新构建游戏**（Debug 配置）
   ```bash
   dotnet build --configuration Debug
   ```

2. **运行游戏并触发晋升事件**
   - 新开剧本或读档
   - 等待势力晋升事件触发
   - 观察弹窗内容

3. **查看 DEBUG 输出**
   - Visual Studio：查看"输出"窗口
   - 或者运行时查看控制台输出

4. **提供 DEBUG 日志**
   - 复制完整的 DEBUG 输出
   - 特别关注 `========== 晋升事件开始 ==========` 到 `========== 显示弹窗结束 ==========` 之间的内容

---

## 🎯 预期的 DEBUG 输出

### 正常情况（君主本人）

```
========== 晋升事件开始 ==========
[Advancement] 势力=曹操军(ID:1)
[Advancement] 君主=曹操(ID:100)
[Advancement] 当前爵位=3, 新爵位=魏王
[Advancement] 是否玩家势力=True
========== 晋升事件结束 ==========
[xianshishijiantupian] 人物=曹操(ID:100), TextResultString=魏王, 分支=LeaderAdvancement
[SetGameObjectBranch] gongfang=Person, Name=曹操
[SetGameObjectBranch] gameObject=Person
[SetGameObjectBranch] branchName=LeaderAdvancement
[GameObjectAndBranchName] 传入对象类型=Person
[GameObjectAndBranchName] 是否Person=True
[GameObjectAndBranchName] person.Name=曹操
[GameObjectAndBranchName] branchName=LeaderAdvancement
========== 显示弹窗 ==========
[DequeueAndDisplay] 队列长度=1
[DequeueAndDisplay] 说话人=曹操(ID:100)
[DequeueAndDisplay] 分支=LeaderAdvancement
[DequeueAndDisplay] 文本=承蒙朝廷恩准，今日孤受封为魏王。望诸君与我同心，共济天下！
========== 显示弹窗结束 ==========
```

### 异常情况（传令官）

```
========== 晋升事件开始 ==========
[Advancement] 势力=曹操军(ID:1)
[Advancement] 君主=曹操(ID:100)
[Advancement] 当前爵位=3, 新爵位=魏王
[Advancement] 是否玩家势力=True
========== 晋升事件结束 ==========
[xianshishijiantupian] 人物=曹操(ID:100), TextResultString=魏王, 分支=LeaderAdvancement
[SetGameObjectBranch] gongfang=???, Name=???  ← 关键：这里应该是什么？
[SetGameObjectBranch] gameObject=???
[SetGameObjectBranch] branchName=???
[GameObjectAndBranchName] 传入对象类型=???
[GameObjectAndBranchName] 是否Person=???
[GameObjectAndBranchName] person.Name=传令官  ← 关键：为什么是传令官？
[GameObjectAndBranchName] branchName=???
========== 显示弹窗 ==========
[DequeueAndDisplay] 队列长度=???
[DequeueAndDisplay] 说话人=传令官(ID:7007)  ← 关键：传令官从哪来的？
[DequeueAndDisplay] 分支=???
[DequeueAndDisplay] 文本=???
========== 显示弹窗结束 ==========
```

---

## 📚 相关文档

- [复用现有功能规范](.codex/复用现有功能规范.md)
- [数据加载问题诊断规范](.codex/数据加载问题诊断规范.md)
- [ANTI-BAND-AID 协议](.agent/rules/global-rules.md)

---

**最后更新：** 2026-03-23  
**维护者：** Lead Architect  
**状态：** 等待用户提供 DEBUG 日志
