# 残留能量机制实施可行性调查

**日期：** 2026-03-21  
**调查人：** Lead Architect  
**目的：** 评估实现"余威能量"（Residual Energy）机制的技术难度和影响范围

---

## 📋 需求描述

### 用户需求（原话）

> "有问题，应该单独有个残留能量，记录某些地块的残留，怎样定义这个残留能量呢，就是跟能量来源无法建立直接联系的能量，也就是现在会消失的能量，不知道我这种描述你能否理解，就跟水流一样，中间被截断了，但某个点上还残留了一些水，之后如果没有截断了，那新的能量覆盖残留，如果一直被截断状态，这些能量就不断消失，这个设想能够实现吗"

### 需求理解

**类比：水流系统**
- **活水**：与水源（城池/部队）有直接连接的能量
- **死水**：与水源断开连接的残留能量
- **截断**：部队离开、城池被占领、路径被敌军阻断
- **衰减**：死水逐渐蒸发（残留能量逐渐消失）
- **覆盖**：新的活水流入，覆盖旧的死水

### 游戏意义

1. **战术深度**：部队撤退后，原地仍有短暂的威慑力（余威）
2. **视觉反馈**：水墨渲染器显示能量逐渐消散的过程
3. **战略价值**：快速推进 vs 稳扎稳打的权衡

---

## 🔍 当前代码结构调查

### 1. 数据结构：TileInfluenceState

**文件位置：** `WorldOfTheThreeKingdoms/GameManager/TileInfluenceState.cs`

**当前结构：**
```csharp
public struct TileInfluenceState
{
    // 基岩层：战略归属（城池）
    public int CityFactionId;
    public int CityEnergy;
    
    // 表土层：战术控制（部队）
    public int ArmyFactionId;
    public int ArmyEnergy;
    
    // 计算属性
    public readonly int TerritoryOwner { get; }
    public readonly int TacticalController { get; }
    public readonly int EffectiveTotalEnergy { get; }
    
    public void Reset();
}
```

**结构大小：**
- 当前：4 个 int 字段 = 16 字节
- 添加残留能量后：6 个 int 字段 = 24 字节
- 增加：50%

**内存影响：**
- 地图大小：63118 格
- 势力数量：假设 10 个
- 当前内存：63118 × 10 × 16 = 10,098,880 字节 ≈ 9.6 MB
- 新增内存：63118 × 10 × 8 = 5,049,440 字节 ≈ 4.8 MB
- 总内存：≈ 14.4 MB

**结论：** ✅ 内存开销可接受

---

### 2. 能量注入逻辑

**文件位置：** `WorldOfTheThreeKingdoms/GameManager/InfluenceUpdateManager.cs`

**关键方法：**
```csharp
private static void InjectTroopEnergies(Faction faction)
{
    // 🔥 当前逻辑：清空旧的部队能量
    for (int i = 0; i < faction.GlobalInfluenceMap.Length; i++)
    {
        if (faction.GlobalInfluenceMap[i].ArmyFactionId == faction.ID)
        {
            faction.GlobalInfluenceMap[i].ArmyFactionId = -1;
            faction.GlobalInfluenceMap[i].ArmyEnergy = 0;
        }
    }
    
    // 然后注入新的部队能量
    // ...
}
```

**需要修改的地方：**
1. 将旧的部队能量转为残留能量（而不是直接清零）
2. 新能量注入时覆盖残留能量

**修改难度：** ⭐⭐ (简单)

---

### 3. 能量衰减逻辑

**文件位置：** `WorldOfTheThreeKingdoms/GameManager/InfluenceUpdateManager.cs`

**关键方法：**
```csharp
private static void DecayFactionEnergy(Faction faction, int armyDecay, int cityDecay)
{
    for (int i = 0; i < faction.GlobalInfluenceMap.Length; i++)
    {
        // 衰减活跃能量
        if (faction.GlobalInfluenceMap[i].ArmyEnergy > 0)
        {
            faction.GlobalInfluenceMap[i].ArmyEnergy -= armyDecay;
            // ...
        }
        
        // 衰减城池能量
        // ...
    }
}
```

**需要修改的地方：**
1. 添加残留能量的衰减逻辑
2. 残留能量衰减速度更快（建议 2 倍）

**修改难度：** ⭐ (非常简单)

---

### 4. 能量竞争逻辑

**文件位置：** `WorldOfTheThreeKingdoms/GameManager/InfluenceUpdateManager.cs`

**关键方法：**
```csharp
private void ApplyGlobalEnergyCompetition()
{
    for (int i = 0; i < totalTiles; i++)
    {
        // 收集所有势力的总能量
        for (int fIdx = 0; fIdx < factionCount; fIdx++)
        {
            int armyEnergy = faction.GlobalInfluenceMap[i].ArmyEnergy;
            int cityEnergy = faction.GlobalInfluenceMap[i].CityEnergy;
            int totalEnergy = armyEnergy + cityEnergy;
            // ...
        }
        
        // 能量竞争逻辑
        // ...
    }
}
```

**需要修改的地方：**
1. 收集能量时包含残留能量
2. 残留能量效果减半（建议 50%）

**修改难度：** ⭐⭐ (简单)

---

### 5. 水墨渲染器

**文件位置：** `WorldOfTheThreeKingdoms/GameManager/InkBleedInfluenceRenderer.cs`

**关键方法：**
```csharp
public void UpdateInfluenceMap()
{
    for (int i = 0; i < _targetInfluenceMap.Length; i++)
    {
        // 查询能量最高的势力
        int maxEnergy = 0;
        int ownerFactionID = -1;
        
        for (int fIdx = 0; fIdx < factionCount; fIdx++)
        {
            int energy = faction.GlobalInfluenceMap[mapIndex].EffectiveTotalEnergy;
            if (energy > maxEnergy)
            {
                maxEnergy = energy;
                ownerFactionID = faction.ID;
            }
        }
        
        _targetInfluenceMap[i] = ownerFactionID;
    }
}
```

**需要修改的地方：**
1. 计算总能量时包含残留能量
2. 残留能量显示为半透明效果（可选）

**修改难度：** ⭐ (非常简单)

---

## 📊 实施难度评估

### 总体难度：⭐⭐ (简单)

| 模块 | 修改难度 | 工作量（小时） | 风险等级 |
|------|---------|--------------|---------|
| TileInfluenceState 结构 | ⭐ | 0.5 | 低 |
| InjectTroopEnergies | ⭐⭐ | 1 | 低 |
| DecayFactionEnergy | ⭐ | 0.5 | 低 |
| ApplyGlobalEnergyCompetition | ⭐⭐ | 1 | 中 |
| InkBleedInfluenceRenderer | ⭐ | 0.5 | 低 |
| 配置参数 | ⭐ | 0.5 | 低 |
| 测试验证 | ⭐⭐⭐ | 2 | 中 |
| **总计** | **⭐⭐** | **6 小时** | **中** |

---

## ⚠️ 潜在问题和风险

### 1. 存档兼容性 ⚠️

**问题：** 修改 `TileInfluenceState` 结构会导致旧存档无法加载

**影响：** 高（用户需要重新开始游戏）

**解决方案：**
1. 添加版本号检查
2. 提供迁移逻辑（旧存档的残留能量初始化为 0）
3. 或者：要求用户重新开始游戏

**建议：** 要求用户重新开始游戏（最简单）

---

### 2. 性能影响 ✅

**问题：** 每个地块增加 2 个 int 字段，增加内存和计算开销

**影响：** 低（内存增加 4.8 MB，计算开销可忽略）

**解决方案：** 无需特殊处理

---

### 3. 逻辑复杂度 ⚠️

**问题：** 残留能量的转换和覆盖逻辑可能引入新的 Bug

**影响：** 中（需要仔细测试）

**解决方案：**
1. 添加详细的注释和文档
2. 编写单元测试
3. 在测试剧本中验证

---

### 4. 视觉反馈 ✅

**问题：** 残留能量的视觉效果需要与活跃能量区分

**影响：** 低（水墨渲染器已支持透明度）

**解决方案：** 残留能量显示为半透明效果（可选）

---

## 🎯 实施建议

### 方案 A：完整实施（推荐）

**优点：**
- 完全实现用户需求
- 视觉效果最佳
- 游戏深度最高

**缺点：**
- 需要 6 小时工作量
- 存档不兼容

**步骤：**
1. 修改 `TileInfluenceState` 结构
2. 修改能量注入逻辑
3. 修改能量衰减逻辑
4. 修改能量竞争逻辑
5. 添加配置参数
6. 修改水墨渲染器（可选）
7. 测试验证

---

### 方案 B：简化实施

**优点：**
- 工作量减少到 3 小时
- 存档兼容

**缺点：**
- 功能不完整
- 视觉效果一般

**步骤：**
1. 不修改 `TileInfluenceState` 结构
2. 使用现有的 `ArmyEnergy` 字段模拟残留能量
3. 修改衰减速度（部队离开后衰减更快）
4. 测试验证

---

### 方案 C：暂不实施

**优点：**
- 无风险
- 无工作量

**缺点：**
- 用户需求未满足

---

## 📝 配置参数设计

### InfluenceConfig.json

```json
{
  "ResidualEnergyConfig": {
    "EnableResidualEnergy": true,
    "ResidualDecayMultiplier": 2.0,
    "ResidualEffectiveness": 0.5,
    "Description": "残留能量配置。EnableResidualEnergy: 是否启用残留能量机制。ResidualDecayMultiplier: 残留能量衰减倍率（相对于活跃能量）。ResidualEffectiveness: 残留能量效果（0.5 = 50%效果）。"
  }
}
```

---

## 🔧 技术细节

### 数据结构修改

```csharp
public struct TileInfluenceState
{
    // 现有字段
    public int CityFactionId;
    public int CityEnergy;
    public int ArmyFactionId;
    public int ArmyEnergy;
    
    // 🆕 残留能量（2026-03-21）
    public int ResidualFactionId;  // -1 表示无残留
    public int ResidualEnergy;     // 残留能量值
}
```

### 能量转换逻辑

```csharp
// 部队离开时，将活跃能量转为残留能量
if (faction.GlobalInfluenceMap[i].ArmyFactionId == faction.ID)
{
    // 转为残留能量
    faction.GlobalInfluenceMap[i].ResidualFactionId = faction.ID;
    faction.GlobalInfluenceMap[i].ResidualEnergy = faction.GlobalInfluenceMap[i].ArmyEnergy;
    
    // 清空活跃能量
    faction.GlobalInfluenceMap[i].ArmyFactionId = -1;
    faction.GlobalInfluenceMap[i].ArmyEnergy = 0;
}
```

### 能量覆盖逻辑

```csharp
// 新能量注入时，覆盖残留能量
if (currentEnergy > faction.GlobalInfluenceMap[currentIndex].ArmyEnergy)
{
    faction.GlobalInfluenceMap[currentIndex].ArmyFactionId = faction.ID;
    faction.GlobalInfluenceMap[currentIndex].ArmyEnergy = currentEnergy;
    
    // 清除残留能量（被新能量覆盖）
    if (faction.GlobalInfluenceMap[currentIndex].ResidualFactionId == faction.ID)
    {
        faction.GlobalInfluenceMap[currentIndex].ResidualFactionId = -1;
        faction.GlobalInfluenceMap[currentIndex].ResidualEnergy = 0;
    }
}
```

---

## 📈 预期效果

### 游戏体验

1. **部队撤退后**：原地仍有短暂的威慑力（余威），敌军不敢立即进入
2. **快速推进**：部队快速移动，留下一条"能量尾迹"，逐渐消散
3. **稳扎稳打**：部队缓慢推进，能量覆盖更稳定，不留残留

### 视觉效果

1. **水墨渲染器**：显示能量逐渐消散的过程（半透明效果）
2. **能量尾迹**：部队移动轨迹清晰可见
3. **战场态势**：更直观地显示战场控制权的变化

---

## ✅ 结论

### 实施可行性：✅ 高

1. **技术难度**：⭐⭐ (简单)
2. **工作量**：6 小时
3. **风险等级**：中（主要是存档兼容性）
4. **预期效果**：优秀

### 推荐方案：方案 A（完整实施）

**理由：**
1. 完全满足用户需求
2. 技术难度可控
3. 工作量合理
4. 游戏体验提升明显

### 前置条件

1. ✅ 用户接受存档不兼容（需要重新开始游戏）
2. ✅ 用户确认配置参数（衰减倍率、效果倍率）
3. ✅ 用户确认是否需要视觉区分（残留能量半透明）

---

**调查完成日期：** 2026-03-21  
**下一步：** 等待用户确认是否实施
