# 硬编码参数提取与配置化建议

## 1. 现状分析
通过对项目的全量检索，发现项目中存在多套配置管理方式并存的情况，同时仍有大量核心数值被硬编码在代码中。

- **现有配置系统**：
  - `GlobalVariables.cs` / `GameParameters.cs`：已有 XML 序列化支持，涵盖了部分全局开关和基础数值。
  - `ConfigManager.cs` (`AIStrategicConfig`)：较新的 JSON 配置系统，用于 AI 宏观战略参数，支持热重载。
- **待提取硬编码**：
  - `GameObjectConsts.cs`：包含约 700 行、数百个 `public const` 常量，涉及战斗计算、AI权重、资源消耗等核心逻辑。
  - **散落的魔法数字**：存在于特定逻辑类（如 `DecisiveBattleManager`）和实体初始化（`Troop`, `Person`）中。

---

## 2. 建议提取的关键参数及代码位置

### 2.1 核心游戏常量 (最紧急)
**位置**: `WorldOfTheThreeKingdoms/GameObjects/GameObjectConsts.cs`
**描述**: 此文件包含了游戏最核心的平衡性数值，目前为编译时常量 (`const`)，修改需重新编译。建议提取为 `GameBalanceConfig.json`。

**关键代码示例 (部分)**:
```csharp
// 战斗与资源倍率
public const float AirRoutewayConsumptionRateMax = 0.7f; // 粮道最大消耗率
public const float FireDamageScale = 0.5f;             // 火灾伤害倍率
public const int ArchitectureDamageConst = 10;         // 建筑基础伤害

// AI 行为权重
public const int AISectionArmyScaleNormalCosnt = 12;   // AI编制军团的标准规模
public const int AISectionOffensiveArmyScaleDivisor = 60; 

// 概率相关
public const int SpySuccessChanceConst = 500;  // 间谍成功率基数
public const int SearchSuccessChanceConst = 1000; // 搜索成功率基数
```

**建议配置结构**:
```json
{
  "Balance": {
    "Routeway": { "ConsumptionMax": 0.7, "ConsumptionLow": 0.3 },
    "Combat": { "FireDamageScale": 0.5, "ArchitectureBaseDamage": 10 },
    "AI": { "ArmyScaleNormal": 12, "OffensiveScaleDivisor": 60 }
  }
}
```

### 2.2 AI 决战逻辑参数
**位置**: `WorldOfTheThreeKingdoms/GameLogic/AI/DecisiveBattleManager.cs`
**描述**: `DetermieWarState` 和 `GetAdjustedAttackThreshold` 方法中存在硬编码的系数。

**关键代码**:
```csharp
// Line 63
return Math.Max(cfg.AggressiveAttackThreshold, standardThreshold * 0.7f); 
// 这里的 0.7f 是硬编码的魔法数字，表示“激进模式下的阈值折扣”
```

**建议**: 将 `0.7f` 提取到 `AIStrategicConfig.cs` 中，命名为 `AggressiveThresholdMultiplier`。

### 2.3 部队 (Troop) 初始化默认值
**位置**: `WorldOfTheThreeKingdoms/GameObjects/Troop.cs`
**描述**: `Init()` 方法中包含大量默认属性赋值，这些值在对象创建时被写死。

**关键代码**:
```csharp
// Line 94 - 130 (Init 方法内)
BaseRateOfQibingDamage = 1;
DefenceRateOnSubdueBubing = 1;
MoraleChangeRateOnOutOfFood = 1;
// ...
// Line 217-226 (IsNewlyCreated 属性)
return (currentTurn - CreationTurn) <= 3; // "3" 是新创建部队保护期，硬编码
```

**建议**:
1. 将 `Init()` 中的默认值提取到 `TroopDefaultConfig`。
2. 将保护期 `3` 提取为 `NewTroopProtectionTurns`。

### 2.4 人物 (Person) 属性影响系数
**位置**: `WorldOfTheThreeKingdoms/GameObjects/Person.cs`
**描述**: 五维属性对各项能力的影响系数目前在 `Init` 中硬编码。

**关键代码**:
```csharp
// Line 150 - 154
InfluenceRateOfCommand = 1.0f;
InfluenceRateOfStrength = 1.0f;
InfluenceRateOfIntelligence = 1.0f;
// ...
```

// ... (Previous content)

### 2.5 战斗计算公式参数
**位置**: `WorldOfTheThreeKingdoms/GameManager/GameMath.cs`
**描述**: `CalculateDamage` 方法中包含硬编码的战斗伤害系数和基准属性值。

**关键代码**:
```csharp
// Line 15
float baseDamage = attacker.FightingForce * (attacker.Morale / 100f) * 0.1f; 
// 0.1f 是基础伤害系数，100f 是士气基准

// Line 18-19
float attackerStats = attacker.Leader != null ? ... : 100f; 
// 100f 是无将领时的默认能力值
```

**建议**: 提取为 `CombatConfig`，包含 `BaseDamageScale`, `MoraleBenchmark`, `DefaultLeaderStats`。

### 2.6 部队 AI 智能评分权重
**位置**: `WorldOfTheThreeKingdoms/GameObjects/Troop.cs`
**描述**: `GetSmartSiegePosition` 方法中包含大量硬编码的评分权重，用于决定 AI 或自动部队的站位偏好。

**关键代码**:
```csharp
// Line 1674-1695
score += minDistToFriendly * 10;
if (isRanged) {
    if (distToCity >= 2) score += 100 + ...; // 远程优先保持距离
    else if (distToCity == 1) score -= 80;   // 远程避免贴脸
}
// ...
if (speed >= 130) ... // 130, 100 是机动性分级阈值
```

**建议**: 提取为 `TroopAIConfig`，将所有评分加减项和阈值配置化，便于调整 AI 智商。

### 2.7 埋伏判定参数
**位置**: `WorldOfTheThreeKingdoms/GameObjects/Troop.cs`
**描述**: `AmbushAvail` 方法中硬编码了视野半径限制。

**关键代码**:
```csharp
// Line 1993
GameArea.GetViewArea(..., (3 > this.ViewRadius) ? this.ViewRadius : 3, ...);
// "3" 是埋伏时的最大视野限制
```

**建议**: 提取为 `StratagemConfig.AmbushMaxViewRadius`。

### 2.8 GlobalVariables 职责分离
**位置**: `WorldOfTheThreeKingdoms/GameGlobal/GlobalVariables.cs`
**描述**: 当前 `GlobalVariables` 混杂了“系统设置”（如音量、全屏、显示网格）和“游戏规则”（如疲劳度增减、处决率、招降率）。

**建议**:
1.  **拆分**: 将游戏规则类参数移入新的 JSON 配置体系（如 `GameRuleConfig`）。
2.  **保留**: `GlobalVariables` 仅保留系统设置（SystemSettings）。
3.  **兼容**: 初期可通过属性包装器保持代码兼容，例如：
    ```csharp
    public int TirednessIncrease => Config.Rules.TirednessIncrease;
    ```

// ... (Previous content)

### 2.9 AI 人员管理 (AI_Personnel_Management)
**位置**: `WorldOfTheThreeKingdoms/GameObjects/AI_Personnel_Management_V8.1_Enhanced.cs`
**描述**: 包含大量用于决定后方治所、前线调动、核心城市选拔的硬编码参数。

**关键代码**:
```csharp
// 核心城市数量公式
int targetCores = (strategicTotal + 2) / 5;

// 评分阈值与权重
if (arch.Population >= 3000 || score > 80f) ...
if (arch.HasHostileTroopsInView()) score += 100f;
if (arch.FrontLine) score += 50f;
if (arch == this.Capital) score += 30f;

// 理想配额
idealQuotas[arch] = 4; // 基础配额
```

**建议**:
1.  提取权重配置 `AIPersonnelConfig`: `FrontLineScore`, `CapitalScore`, `PopulationThreshold`.
2.  提取公式参数: `CoreCityDivisor` (5), `BaseQuota` (4).

### 2.10 UI 插件布局 (TroopDetailPlugin)
**位置**: `WorldOfTheThreeKingdoms/GamePlugins/TroopDetailPlugin/TroopDetail.cs`
**描述**: 包含硬编码的 UI 布局坐标、尺寸颜色及绘制深度。

**关键代码**:
```csharp
// 绘制深度
CacheManager.Draw(..., 0.2f); // 背景深度
TroopNameText.Draw(0.1999f);  // 文字深度

// 布局尺寸 (虽然部分被注释，但代表了典型硬编码风格)
int portraitSize = 60;
int titleHeight = 18;
```

**建议**: 提取为 `TroopDetailLayout.json`，定义 `ZOrder` (绘制深度), `ElementBoundaries` (各元素位置), `Colors`.

### 2.11 特性 ID 逻辑耦合 (InfluenceKind)
**位置**: `WorldOfTheThreeKingdoms/GameObjects/Influences/InfluenceKind.cs`
**描述**: 代码逻辑直接绑定了特定 ID 的特性（Magic Numbers），导致新增特性若不修改代码将无法生效或生效错误。

**关键代码**:
```csharp
// Line 555
if (i.appliedTroop.Add(...) || (this.ID >= 390 && this.ID <= 399) || this.ID == 720 || this.ID == 721)
```

**建议**:
1.  **消除 ID 耦合**: 在 `InfluenceKind` 中增加布尔属性标记，例如 `bool AlwaysReapply`, `bool IsSpecialCombatEffect`。
2.  **数据驱动**: 在 JSON 数据中设置这些属性，而不是在 C# 代码中判断 ID。

---

---

## 3. 参数分类与文件结构分配方案

为了实现高内聚、低耦合的配置管理，建议将提取的参数分配到以下 5 个 JSON 配置文件中。这种结构基于“功能模块”而非“代码位置”进行划分，更符合策划配置习惯。

### 3.1 `GlobalConfig.json` (系统与全局设置)
**用途**: 管理游戏系统级参数，不直接涉及战斗数值平衡。
- **SystemSettings**:
    - 音量 (MusicVolume, SoundVolume)
    - 分辨率与全屏 (Resolution, FullScreen)
    - 自动保存频率 (AutoSaveFrequency)
    - 游戏速度 (GameSpeed)
- **MapSettings**:
    - 网格显示开关 (DrawGrid)
    - 地图边缘滚动速度 (ScrollSpeed)

### 3.2 `GameBalance.json` (核心数值平衡)
**用途**: 核心游戏性参数，直接影响战斗、内政和资源产出。此类参数调整最为频繁。
- **Combat (战斗公式)**:
    - 基础伤害系数 (`BaseDamageScale`: 0.1)
    - 士气修正基准 (`MoraleBenchmark`: 100)
    - 火灾伤害倍率 (`FireDamageScale`: 0.5)
    - 建筑基础伤害 (`ArchitectureBaseDamage`: 10)
- **March (行军与消耗)**:
    - 粮道消耗系数 (`RoutewayConsumptionMax`: 0.7)
    - 疲劳度增长率 (`TirednessIncrease`)
    - 缺粮士气下降率 (`MoraleChangeRateOnOutOfFood`)
- **Economy (经济与内政)**:
    - 搜索成功率基数 (`SearchSuccessChance`)
    - 间谍成功率基数 (`SpySuccessChance`)

### 3.3 `AIConfig.json` (人工智能行为)
**用途**: 控制 AI 的决策逻辑、进攻欲望和资源分配偏好。
- **Strategic (宏观战略)**:
    - 进攻阈值乘数 (`AggressiveThresholdMultiplier`: 0.7) - *来自 DecisiveBattleManager*
    - 军团标准规模 (`ArmyScaleNormal`: 12)
    - 进攻规模除数 (`OffensiveScaleDivisor`: 60)
- **Personnel (人员管理)**: - *来自 AI_Personnel_Management*
    - 核心城市数量除数 (`CoreCityDivisor`: 5)
    - 基础配额 (`BaseQuota`: 4)
    - 前线城市评分权重 (`FrontLineScoreWeight`: 50)
- **TroopAI (部队行为)**: - *来自 Troop.cs*
    - 智能攻城评分权重 (友军距离/机动性阈值)
    - 撤退生命值阈值

### 3.4 `ObjectDefaults.json` (对象默认值)
**用途**: 定义新对象（部队、武将）创建时的初始状态。
- **Troop (部队)**:
    - 新部队保护期回合数 (`NewTroopProtectionTurns`: 3)
    - 默认防御率/战法成功率
- **Person (武将)**:
    - 默认五维影响系数 (`ImpactRates`: Command/Strength/Intelligence/Politics/Glamour 均为 1.0)
- **Stratagem (计略)**:
    - 埋伏最大视野半径 (`AmbushMaxViewRadius`: 3)

### 3.5 `UIConfig.json` (界面布局与表现)
**用途**: 将硬编码的 UI 坐标、颜色和绘制层级提取出来，支持界面微调。
- **TroopDetail (部队详情)**:
    - 头像尺寸 (`PortraitSize`: 60)
    - 称号条高度 (`TitleHeight`: 18)
    - 绘制层级 (`ZOrders`: Background 0.2, Text 0.1999)
- **TabList (列表控件)**:
    - 列宽计算参数

---

## 4. 后续实施路径 (Roadmap)

1.  **准备阶段**: 扩展 `ConfigManager`，支持加载上述多文件结构。
2.  **第一阶段 (Low Risk)**: 迁移 `UIConfig` 和 `AIConfig` (AI参数通常不破坏存档兼容性)。
3.  **第二阶段 (Core Logic)**: 迁移 `GlobalConfig` 和 `ObjectDefaults`，替换 `GlobalVariables` 和 `GameObjectConsts`。
4.  **第三阶段 (High Risk)**: 迁移 `GameBalance`，重构战斗计算公式 (`GameMath`) 以读取配置。

此方案在不修改代码逻辑的前提下，清晰地梳理了参数的归属，为后续重构提供了蓝图。
