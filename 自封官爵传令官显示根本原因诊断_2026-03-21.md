# 自封官爵/晋升弹窗仍显示传令官：修复记录与推断（截至 2026-03-22）

日期：2026-03-22  
状态：仍未修复（用户反馈：重启游戏后依然无变化）

## 现象

玩家势力在“自封官爵/官爵晋升”相关事件中弹出的 `tupianwenzi` 对话框，头像/标题仍显示为“传令官”，而不是当前势力君主本人。

## 关键事实（代码审查结论）

- UI 不会“自动把君主换成传令官”。`MGSPersonText.xianshishijiantupian(...)` 与 `tupianwenzi` 插件会直接使用传入的 `Person p` 作为 `SpeakingPerson`，并用 `p.Name` 填充 UI。
- 官爵晋升并不总是走 `SelfAdvancement()`。
  - 在 `Faction.shizheshengguan()` 的分流下：
    - 有皇帝、非异族、朝廷贡献度与城池数达标时走 `Advancement()`（朝廷加封口径）
    - 其他情况走 `SelfAdvancement()`（自封口径）

## 2026-03-21 版本的仓内结论为什么会偏

仓内旧文档将根因指向 `IsPlayer()` 失效并聚焦修 `SelfAdvancement()` 与 `Zili` 文案。

但从代码路径上看，很多玩家遇到的晋升弹窗更可能来自 `Advancement()`，因此只改 `SelfAdvancement()` 经常不会生效。

## 我们实际做过的修复尝试（2026-03-22）

### 1) 改 `Advancement()` 的“弹窗显示链路”（不改晋升判定，不改年表/简报/事件）

文件：`G:\sanguo\sanguo260320-2\WorldOfTheThreeKingdoms\GameObjects\Faction.cs`

- 针对“当前玩家势力是否显示弹窗”的判断从 `IsPlayer(this)` 改为 `CurrentPlayer != null && CurrentPlayer == this`，避免玩家势力识别分歧导致不弹窗。
- 弹窗发言人固定使用 `this.Leader`（君主本人）。
- 弹窗分支不再走 `shengguan`，改为新的 `LeaderAdvancement`（让文案口径也变成“君主受封”，避免玩家体感仍像传令官宣旨）。
- 简报/年表/事件分发逻辑保持原样（仍写 `shengguan` 简报与年表，仍 RaiseAdvancement），只动“玩家看到的那一帧 UI”。

### 2) 新增 `LeaderAdvancement` 文案分支（两份文本树都补）

文件：

- `G:\sanguo\sanguo260320-2\Content\Data\Plugins\tupianwenziTextTree.xml`
- `G:\sanguo\sanguo260320-2\Content\Textures\GameComponents\tupianwenzi\Data\tupianwenziTextTree.xml`

新增分支名：`LeaderAdvancement`

### 3) 构建验证（仅证明产物已更新，不证明游戏一定跑到这条路径）

- `Debug` 与 `Release` 均 `dotnet build` 通过（0 error）。
- 输出目录中的 `tupianwenziTextTree.xml` 已包含 `LeaderAdvancement`。

## 当前结论（用户反馈）

用户反馈“重新打开游戏后依然是传令官对话框，修复无效”，因此：

- 要么玩家实际运行的不是本仓构建输出（运行了其他目录/旧版本/发布包）
- 要么玩家看到的弹窗不是 `Advancement()/SelfAdvancement()` 这条晋升链路触发（例如走了其他事件，仍固定使用了传令官/中立人）
- 要么运行时 `Content` 来源被外部覆盖（导致我们改的 XML 与代码不在运行路径上）

以上属于推断，按用户要求此处先不继续展开调查与验证。

## （可选）最小自检清单

不做进一步调查的前提下，用户可自行快速确认两点：

1. 运行中的 `WorldOfTheThreeKingdoms.exe` 的实际路径（任务管理器“打开文件所在位置”）。
2. 该路径下的 `Content\Data\Plugins\tupianwenziTextTree.xml` 是否包含 `LeaderAdvancement`。

