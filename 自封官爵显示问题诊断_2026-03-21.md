# 自封官爵显示问题诊断报告

**日期：** 2026-03-21  
**问题：** 修复后仍然显示传令官对话框  
**状态：** 🔍 诊断中

---

## 🔍 问题现象

用户反馈：修复后仍然"只弹出一个传令官的对话框"，没有任何上下文。

---

## 📋 代码检查结果

### ✅ 代码已正确修复

**文件：** `WorldOfTheThreeKingdoms/GameObjects/Faction.cs` (第 9811-9860 行)

```csharp
private void SelfAdvancement()
{
    this.guanjue++;
    guanjuezhongleilei gj2 = Session.Current.Scenario.GameCommonData.suoyouguanjuezhonglei.Getguanjuedezhonglei(this.guanjue);
    
    if (gj2 != null && Session.Current.Scenario.IsPlayer(this))
    {
        // ✅ 正确：使用君主本人
        Session.MainGame.mainGameScreen.xianshishijiantupian(
            this.Leader,                        // 君主本人
            gj2.Name,                           // 官爵名称
            "Zili",                             // 分支名
            "shengguan.jpg",                    // 图片
            "",                                 // 音效
            true);                              // 总是显示
    }
    
    // 记录简报和年表
    if (gj2 != null)
    {
        Session.MainGame.mainGameScreen.xiejinxingjilu("Zili", this.LeaderName,
            gj2.Name, this.Leader.Position);
        Session.Current.Scenario.YearTable.addSelfAdvanceGuanjueEntry(Session.Current.Scenario.Date, this, gj2);
    }
    
    // 触发外交事件
    WorldOfTheThreeKingdoms.GameObjects.Events.DiplomacyEvents.RaiseSelfAdvancement(Session.Current.Scenario, this);
}
```

**检查结果：** ✅ 代码正确，使用 `this.Leader` 而不是传令官

---

### ✅ XML 配置已正确修复

**文件：** `Content/Data/Plugins/tupianwenziTextTree.xml` (第 550-555 行)

```xml
<Branch Name="Zili">
  <Leaf Text="承蒙各位爱戴，今日孤自封为" Color="4294967295"></Leaf>
  <Leaf Property="TextResultString" Color="4278255360"></Leaf>  <!-- 官爵名称 -->
  <Leaf Text="。我们齐心协力，共创大业！" Color="4294967295"></Leaf>
</Branch>
```

**检查结果：** ✅ XML 正确，使用 `TextResultString` 显示官爵名称

---

## 🚨 可能的原因

### 原因 1：代码未重新编译 ⚠️

**症状：** 修改了代码，但游戏仍然运行旧版本

**检查方法：**
1. 查看 `bin/Debug/net8.0/WorldOfTheThreeKingdoms.dll` 的修改时间
2. 确认是否在代码修改之后

**解决方案：**
```bash
# 清理并重新编译
dotnet clean
dotnet build
```

---

### 原因 2：XML 文件未正确部署 ⚠️

**症状：** 代码正确，但 XML 文件是旧版本

**检查方法：**
1. 打开 `bin/Debug/net8.0/Content/Data/Plugins/tupianwenziTextTree.xml`
2. 查找 "Zili" 分支（第 550 行左右）
3. 确认是否使用 `TextResultString` 而不是 `TextDestinationString`

**预期内容：**
```xml
<Branch Name="Zili">
  <Leaf Text="承蒙各位爱戴，今日孤自封为" Color="4294967295"></Leaf>
  <Leaf Property="TextResultString" Color="4278255360"></Leaf>  <!-- ✅ 正确 -->
  <Leaf Text="。我们齐心协力，共创大业！" Color="4294967295"></Leaf>
</Branch>
```

**错误内容（旧版本）：**
```xml
<Branch Name="Zili">
  <Leaf Property="TextResultString" Color="4286644223"></Leaf>  <!-- 君主名字 -->
  <Leaf Text="承蒙各位爱戴，今日我自封为" Color="4294967295"></Leaf>
  <Leaf Property="TextDestinationString" Color="4278255360"></Leaf>  <!-- ❌ 错误 -->
  <Leaf Text="。我们齐心协力，共创大业！" Color="4294967295"></Leaf>
</Branch>
```

**解决方案：**
```bash
# 手动复制 XML 文件到 bin 目录
copy "Content\Data\Plugins\tupianwenziTextTree.xml" "bin\Debug\net8.0\Content\Data\Plugins\tupianwenziTextTree.xml"
```

---

### 原因 3：游戏缓存了旧数据 ⚠️

**症状：** 代码和 XML 都正确，但游戏仍然使用旧数据

**解决方案：**
1. 完全退出游戏
2. 删除缓存文件（如果存在）
3. 重新启动游戏

---

### 原因 4：AI 势力自封触发了对话框 ⚠️

**症状：** 看到传令官对话框，但不是玩家势力自封

**分析：**
代码中有这个检查：
```csharp
if (gj2 != null && Session.Current.Scenario.IsPlayer(this))
{
    // 只对玩家势力显示对话框
}
```

**可能的情况：**
- 用户看到的是 AI 势力自封的简报记录，而不是对话框
- 或者是其他事件（如朝廷晋升）触发的对话框

**检查方法：**
1. 确认是否是玩家势力自封（如曹操）
2. 还是 AI 势力自封（如孙权、刘备）
3. 查看简报记录，确认事件类型

---

## 🧪 测试步骤

### 步骤 1：确认编译

```bash
# 清理项目
dotnet clean

# 重新编译
dotnet build

# 检查 DLL 时间戳
dir bin\Debug\net8.0\WorldOfTheThreeKingdoms.dll
```

**预期结果：** DLL 的修改时间应该是最近的时间

---

### 步骤 2：确认 XML 部署

```bash
# 检查 bin 目录下的 XML 文件
type "bin\Debug\net8.0\Content\Data\Plugins\tupianwenziTextTree.xml" | findstr /C:"Zili" /A:5
```

**预期结果：** 应该看到正确的 "Zili" 分支内容

---

### 步骤 3：游戏内测试

1. 启动游戏
2. 选择曹操势力
3. 等待季度末（3/6/9/12月）
4. 确保满足晋升条件：
   - 城池数达到下一级官爵要求
   - （汉族势力）朝廷贡献度达标
5. 观察事件显示

**预期结果：**
- ✅ 显示曹操的头像（不是传令官）
- ✅ 对话框标题显示"曹操"
- ✅ 对话内容："承蒙各位爱戴，今日孤自封为魏王。我们齐心协力，共创大业！"

---

### 步骤 4：添加调试日志

如果问题仍然存在，可以添加调试日志：

**修改 `Faction.cs` 第 9820 行：**
```csharp
if (gj2 != null && Session.Current.Scenario.IsPlayer(this))
{
    // 🔥 调试日志
    System.Diagnostics.Debug.WriteLine($"[SelfAdvancement] 势力={this.Name}, 君主={this.Leader?.Name}, 官爵={gj2.Name}");
    
    if (this.Leader == null)
    {
        throw new InvalidOperationException($"数据损坏：势力 {this.Name} 没有君主");
    }
    
    Session.MainGame.mainGameScreen.xianshishijiantupian(
        this.Leader,
        gj2.Name,
        "Zili",
        "shengguan.jpg",
        "",
        true);
}
```

**查看调试输出：**
- 在 Visual Studio 的"输出"窗口查看调试信息
- 确认是否调用了正确的方法

---

## 🎯 快速修复指南

### 方案 1：完全重新编译

```bash
# 1. 清理项目
dotnet clean

# 2. 删除 bin 和 obj 目录
rmdir /s /q bin
rmdir /s /q obj

# 3. 重新编译
dotnet build

# 4. 运行游戏
dotnet run
```

---

### 方案 2：手动部署 XML 文件

```bash
# 确保 XML 文件被复制到 bin 目录
copy "Content\Data\Plugins\tupianwenziTextTree.xml" "bin\Debug\net8.0\Content\Data\Plugins\tupianwenziTextTree.xml" /Y
```

---

### 方案 3：检查 .csproj 文件

确认 XML 文件被设置为"复制到输出目录"：

**打开 `WorldOfTheThreeKingdoms.csproj`，查找：**
```xml
<ItemGroup>
  <Content Include="Content\Data\Plugins\tupianwenziTextTree.xml">
    <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
  </Content>
</ItemGroup>
```

如果没有，添加这个配置。

---

## 📊 诊断检查清单

请按以下清单逐项检查：

- [ ] 1. 代码是否已修改（`Faction.cs` 第 9830 行使用 `this.Leader`）
- [ ] 2. XML 是否已修改（`tupianwenziTextTree.xml` 第 552 行使用 `TextResultString`）
- [ ] 3. 项目是否已重新编译（`dotnet build`）
- [ ] 4. DLL 时间戳是否是最新的
- [ ] 5. bin 目录下的 XML 文件是否是最新版本
- [ ] 6. 游戏是否完全重启
- [ ] 7. 测试的是否是玩家势力（不是 AI 势力）
- [ ] 8. 是否是自封官爵事件（不是朝廷晋升）

---

## 🔧 如果问题仍然存在

如果按照上述步骤操作后问题仍然存在，请提供以下信息：

1. **DLL 时间戳：**
   ```bash
   dir bin\Debug\net8.0\WorldOfTheThreeKingdoms.dll
   ```

2. **XML 文件内容：**
   ```bash
   type "bin\Debug\net8.0\Content\Data\Plugins\tupianwenziTextTree.xml" | findstr /C:"Zili" /A:5
   ```

3. **调试日志输出：**
   - Visual Studio 输出窗口中的 `[SelfAdvancement]` 日志

4. **截图：**
   - 显示传令官对话框的截图
   - 简报记录的截图

---

## 📚 相关文档

- [自封官爵传令官显示修复_2026-03-20.md](自封官爵传令官显示修复_2026-03-20.md)
- [游戏核心机制规范](.kiro/steering/游戏核心机制.md)
- [ANTI-BAND-AID 协议](.agent/rules/global-rules.md)

---

**诊断完成时间：** 2026-03-21  
**维护者：** Lead Architect  
**下一步：** 等待用户反馈诊断结果
