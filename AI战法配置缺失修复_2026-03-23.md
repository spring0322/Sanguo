# AI 战法配置缺失修复报告

**日期：** 2026-03-23  
**问题：** AI 战术系统在评估战法时找不到 `CombatMethodScoring` 配置  
**状态：** ✅ 已修复

---

## 🚨 问题描述

### 错误堆栈

```
System.Diagnostics.Debug.Assert 失败
[GetCombatMethodScoringConfig] CombatMethodScoring 配置缺失，检查 AITacticalConfig.json

at WorldOfTheThreeKingdoms.GameGlobal.AITacticalConfigManager.GetCombatMethodScoringConfig()
at GameObjects.Troop.CalculateCombatMethodScore(CombatMethod method, Troop target, Point tile)
at GameObjects.Troop.EvaluateCombatMethods(Point tile, List`1 candidates, Boolean canMoveToTile)
at GameObjects.Troop.GetBestCombatPlan()
at GameObjects.Troop.ExecuteSmartTurn()
at GameObjects.Troop.ExecuteTactics()
at GameObjects.Troop.AI()
```

### 触发场景

- AI 势力在战略阶段（`Faction.Run()`）执行军团决策
- 部队 AI（`Troop.AI()`）尝试评估战法选项
- 调用 `AITacticalConfigManager.GetCombatMethodScoringConfig()` 时触发断言失败

---

## 🔍 根本原因

### 配置文件结构错误

**错误的 JSON 结构**（修复前）：

```json
{
  "StratagemScoring": {
    "Description": "计略评分配置",
    // ... 计略配置 ...
    "CombatMethodScoring": {           // ❌ 错误：嵌套在 StratagemScoring 内部
      "Description": "战法评分配置",
      // ...
    },
    "CombatMethodSuccessRate": {       // ❌ 错误：嵌套在 StratagemScoring 内部
      // ...
    },
    "StuntScoring": {                  // ❌ 错误：嵌套在 StratagemScoring 内部
      // ...
    }
  }
}
```

**正确的 JSON 结构**（修复后）：

```json
{
  "StratagemScoring": {
    "Description": "计略评分配置",
    // ... 计略配置 ...
  },
  "CombatMethodScoring": {             // ✅ 正确：顶层配置节
    "Description": "战法评分配置",
    // ...
  },
  "CombatMethodSuccessRate": {         // ✅ 正确：顶层配置节
    // ...
  },
  "StuntScoring": {                    // ✅ 正确：顶层配置节
    // ...
  }
}
```

### 为什么会出现这个问题？

**配置类定义**（`AITacticalConfigManager.cs`）：

```csharp
public class AITacticalConfig
{
    public TacticalPositioningConfig TacticalPositioning { get; set; }
    public StratagemScoringConfig StratagemScoring { get; set; }
    public FriendlyStratagemScoringConfig FriendlyStratagemScoring { get; set; }
    public StratagemSuccessRateConfig StratagemSuccessRate { get; set; }
    public CombatMethodScoringConfig CombatMethodScoring { get; set; }      // 🔥 顶层属性
    public CombatMethodSuccessRateConfig CombatMethodSuccessRate { get; set; }  // 🔥 顶层属性
    public StuntScoringConfig StuntScoring { get; set; }                    // 🔥 顶层属性
    public LegionFormationConfig LegionFormation { get; set; }
}
```

**问题**：
- JSON 文件中 `CombatMethodScoring`、`CombatMethodSuccessRate`、`StuntScoring` 被错误地嵌套在 `StratagemScoring` 内部
- System.Text.Json 反序列化时无法找到这些顶层属性
- 结果：`Config.CombatMethodScoring == null`
- 触发：`Debug.Assert` 失败

---

## ✅ 修复方案

### 修改文件

**文件**：`Content/Data/AITacticalConfig.json`

**修改内容**：
- 将 `CombatMethodScoring` 从 `StratagemScoring` 内部移到顶层
- 将 `CombatMethodSuccessRate` 从 `StratagemScoring` 内部移到顶层
- 将 `StuntScoring` 从 `StratagemScoring` 内部移到顶层
- 将 `FriendlyStratagemScoring` 从 `StratagemScoring` 内部移到顶层
- 将 `StratagemSuccessRate` 从 `StratagemScoring` 内部移到顶层

### 修复后的配置结构

```json
{
  "TacticalPositioning": { /* ... */ },
  "StratagemScoring": {
    "Description": "计略评分配置（2026-03-09）",
    "BaseScoreMultiplier": 10.0,
    "ConservativeModifier": 0.8,
    "RoleModifiers": { /* ... */ },
    "MoraleModifiers": { /* ... */ },
    "ExecutionModifier": { /* ... */ },
    "AOEScoring": { /* ... */ }
  },
  "FriendlyStratagemScoring": { /* ... */ },
  "StratagemSuccessRate": { /* ... */ },
  "CombatMethodScoring": {
    "Description": "战法评分配置 - 2026-03-09",
    "BaseScoreMultiplier": 10.0,
    "ConservativeModifier": 1.0,
    "RoleModifiers": { /* ... */ },
    "CombativityModifiers": { /* ... */ },
    "ExecutionModifier": { /* ... */ }
  },
  "CombatMethodSuccessRate": { /* ... */ },
  "StuntScoring": { /* ... */ },
  "LegionFormation": { /* ... */ }
}
```

---

## 🔧 验证步骤

### 1. 编译检查

```bash
dotnet build
```

**预期结果**：✅ 编译成功，无错误

### 2. 运行时验证

**测试场景**：
1. 启动游戏
2. 开始新剧本（184 年大汉中兴）
3. 点击"进行"按钮，进入执行阶段
4. 观察 AI 势力是否正常执行战术决策

**预期结果**：
- ✅ AI 部队正常评估战法
- ✅ 无 `Debug.Assert` 失败
- ✅ 无配置缺失错误

### 3. 配置加载验证

**添加调试日志**（可选）：

```csharp
// AITacticalConfigManager.cs - Initialize() 方法
public new void Initialize()
{
    base.Initialize();
    ValidateConfig(Config);
    
    #if DEBUG
    System.Diagnostics.Debug.WriteLine($"[AITacticalConfigManager] 配置加载成功");
    System.Diagnostics.Debug.WriteLine($"  - CombatMethodScoring: {Config.CombatMethodScoring != null}");
    System.Diagnostics.Debug.WriteLine($"  - CombatMethodSuccessRate: {Config.CombatMethodSuccessRate != null}");
    System.Diagnostics.Debug.WriteLine($"  - StuntScoring: {Config.StuntScoring != null}");
    #endif
}
```

**预期输出**：
```
[AITacticalConfigManager] 配置加载成功
  - CombatMethodScoring: True
  - CombatMethodSuccessRate: True
  - StuntScoring: True
```

---

## 📋 相关配置节说明

### CombatMethodScoring（战法评分配置）

**用途**：AI 评估战法（如"突击"、"强攻"）的评分参数

**关键参数**：
- `BaseScoreMultiplier`：基础评分倍率（10.0）
- `RoleModifiers`：角色修正（Mage: 1.5, DPS: 1.2）
- `CombativityModifiers`：战意修正（高战意加成，低战意惩罚）
- `ExecutionModifier`：斩杀修正（敌人兵力过低时降低评分，节省战意）

### CombatMethodSuccessRate（战法成功率配置）

**用途**：AI 预判战法成功率（统率对抗系统）

**关键参数**：
- `BaseSuccessRate`：基础成功率（0.7）
- `CommandInfluence`：统率影响系数（0.015）
- `CounterThreshold`：统率差阈值（-20）
- `MinSuccessRate` / `MaxSuccessRate`：成功率上下限

### StuntScoring（特技评分配置）

**用途**：AI 评估特技（如"火神"、"水神"）的评分参数

**关键参数**：
- `PowerMultiplierBase`：威力基础倍率（1.0）
- `PowerMultiplierPerInfluence`：每个影响格的威力加成（0.5）
- `RoleModifiers`：角色修正（Mage: 1.6, DPS: 1.4）
- `InfluenceCountModifier`：影响格数量修正（0.15）

---

## 🎯 教训总结

### 1. 配置文件结构必须与类定义匹配

**规则**：
- JSON 文件的顶层属性必须与 `AITacticalConfig` 类的属性一一对应
- 嵌套结构必须与类的嵌套结构完全一致
- System.Text.Json 不会自动"展平"嵌套结构

**检查方法**：
```csharp
// 1. 查看类定义
public class AITacticalConfig
{
    public CombatMethodScoringConfig CombatMethodScoring { get; set; }  // 顶层属性
}

// 2. 确认 JSON 结构
{
  "CombatMethodScoring": { /* ... */ }  // 必须是顶层节点
}
```

### 2. 配置缺失时使用 Fail Fast

**正确的做法**（项目已采用）：

```csharp
System.Diagnostics.Debug.Assert(Config.CombatMethodScoring != null,
    "[GetCombatMethodScoringConfig] CombatMethodScoring 配置缺失，检查 AITacticalConfig.json");
```

**优点**：
- 立即发现配置问题
- 提供明确的错误信息
- 避免运行时出现更严重的错误

### 3. 配置文件修改后必须验证

**验证步骤**：
1. ✅ JSON 格式验证（使用 JSON 验证工具）
2. ✅ 编译检查（确认配置类定义匹配）
3. ✅ 运行时验证（启动游戏，观察配置加载）

---

## 📚 相关文档

- [AI 战法系统完整实施总结](修改记录/AI战法系统完整实施总结_2026-03-09.md)
- [AI 特技系统配置化评分实施完成](修改记录/AI特技系统配置化评分实施完成_2026-03-10.md)
- [数据加载问题诊断规范](.codex/数据加载问题诊断规范.md)

---

**最后更新：** 2026-03-23  
**维护者：** Lead Architect  
**修复时间：** 约 5 分钟（诊断 + 修复 + 验证）
