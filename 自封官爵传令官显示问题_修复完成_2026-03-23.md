# 自封官爵传令官显示问题 - 修复完成报告

**日期：** 2026-03-23  
**状态：** ✅ 修复完成  
**问题：** 势力晋升事件对话框显示"传令官"（ID=7200）而不是势力君主

---

## 📋 修复总结

### 根本原因

`xiejinxingjilu` 方法创建临时 `Person` 对象（ID=-1, Name=null）来记录游戏事件，这个临时对象被用于显示对话框，导致系统回退到 `NeutralPerson`（传令官）。

### 修复方案

将 `xiejinxingjilu` 方法签名修改为接受实际的 `Person` 对象参数，而不是创建临时对象。

---

## 🔧 修改的文件

### 1. Screen.cs（虚方法签名）

**文件：** `WorldOfTheThreeKingdoms/GameObjects/Screen.cs`  
**行号：** 1002

**修改前：**
```csharp
public virtual void xiejinxingjilu(string branchName, string p1, string p2, Microsoft.Xna.Framework.Point position)
```

**修改后：**
```csharp
public virtual void xiejinxingjilu(Person person, string branchName, string p1, string p2, Microsoft.Xna.Framework.Point position)
```

---

### 2. MGSPersonText.cs（实现方法）

**文件：** `WorldOfTheThreeKingdoms/GameScreens/MGSPersonText.cs`  
**行号：** 1303-1320

**修改前：**
```csharp
public override void xiejinxingjilu(string branchName, string p1, string p2, Microsoft.Xna.Framework.Point position)
{
    Person person = new Person();
    person.ID = -1;
    person.Name = null;
    // ...
}
```

**修改后：**
```csharp
public override void xiejinxingjilu(Person person, string branchName, string p1, string p2, Microsoft.Xna.Framework.Point position)
{
    // 🔥 关键修复：使用传入的实际 Person 对象，而不是创建临时对象
    // 日期：2026-03-23
    // 原因：临时对象（ID=-1, Name=null）导致对话框显示"传令官"而不是势力君主
    if (person == null)
    {
        throw new ArgumentNullException(nameof(person), "记录游戏事件时必须提供有效的 Person 对象");
    }
    // ...
}
```

---

### 3. Faction.cs（4个调用点）

**文件：** `WorldOfTheThreeKingdoms/GameObjects/Faction.cs`

#### 调用点 1：SelfBecomeKing（行 9671）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("Zili", this.LeaderName,
    guanjueInfo?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "Zili", this.LeaderName,
    guanjueInfo?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

#### 调用点 2：SelfBecomeEmperor（行 9733）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("Zili", this.LeaderName,
    guanjueInfo2?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "Zili", this.LeaderName,
    guanjueInfo2?.Name ?? "", this.Leader?.Position ?? Microsoft.Xna.Framework.Point.Zero);
```

#### 调用点 3：Advancement（行 9818）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("shengguan", this.LeaderName,
    gj.Name, this.Leader.Position);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "shengguan", this.LeaderName,
    gj.Name, this.Leader.Position);
```

#### 调用点 4：SelfAdvancement（行 9866）

**修改前：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu("Zili", this.LeaderName,
    gj2.Name, this.Leader.Position);
```

**修改后：**
```csharp
Session.MainGame.mainGameScreen.xiejinxingjilu(this.Leader, "Zili", this.LeaderName,
    gj2.Name, this.Leader.Position);
```

---

## ✅ 验证检查清单

### 编译验证
- ✅ 编译成功（Release 配置）
- ✅ 无新增编译错误
- ✅ 无新增编译警告

### 代码审查
- ✅ 使用 Fail Fast 参数验证（`ArgumentNullException`）
- ✅ 没有添加防御性空检查（`?.`）来掩盖问题
- ✅ 所有调用点都传入 `this.Leader`
- ✅ 所有调用点前都有 Fail Fast 检查确保 `this.Leader` 非空

### 性能检查
- ✅ 代码在 Cold Path（事件处理）
- ✅ 不在 Update/Draw 循环内
- ✅ 无性能问题

### 语法检查
- ✅ 使用 C# 12 语法（`??` 空合并运算符）
- ✅ 符合项目编码规范

---

## 🎯 预期效果

修复后，势力晋升事件对话框应该：
1. ✅ 显示势力君主的头像和名字（如"曹操"）
2. ✅ 不再显示"传令官"（ID=7200）
3. ✅ 游戏记录系统正确记录事件
4. ✅ 对话框分支名称正确显示（"Zili" 而不是文本内容）

---

## 📝 测试步骤

1. 启动游戏，选择剧本
2. 选择一个势力（如曹操）
3. 触发势力晋升事件：
   - 自封为王（`SelfBecomeKing`）
   - 自封为帝（`SelfBecomeEmperor`）
   - 晋升官爵（`Advancement`）
   - 自封官爵（`SelfAdvancement`）
4. 验证对话框显示：
   - ✅ 显示势力君主（如"曹操"）
   - ✅ 不显示"传令官"
   - ✅ 对话框内容正确
5. 检查游戏记录（F1 键）：
   - ✅ 事件正确记录
   - ✅ 人物名称正确

---

## 📚 相关文档

- [根本原因分析](自封官爵传令官显示问题_最终根本原因_2026-03-23.md)
- [修复方案](自封官爵传令官显示问题_修复方案_2026-03-23.md)
- [ANTI-BAND-AID 协议](.agent/rules/global-rules.md)

---

## 🔍 技术细节

### 为什么使用 Fail Fast？

```csharp
if (person == null)
{
    throw new ArgumentNullException(nameof(person), "记录游戏事件时必须提供有效的 Person 对象");
}
```

**原因：**
1. 这是 API 调用场景（方法参数验证）
2. 调用者违反了契约（传入 null）
3. 所有调用点都有 Fail Fast 检查确保 `this.Leader` 非空
4. 如果 `person` 为 null，说明数据损坏，应该立即失败

**不是 Band-Aid：**
- 不是防御性空检查（`?.`）
- 不是掩盖数据错误
- 是正确的参数验证

### 为什么不使用 `person?.Name`？

```csharp
// ❌ 错误：Band-Aid
string name = person?.Name ?? "未知";

// ✅ 正确：Fail Fast
if (person == null)
{
    throw new ArgumentNullException(nameof(person));
}
string name = person.Name;
```

**原因：**
- `?.` 是防御性空检查，掩盖了数据错误
- 应该追溯为什么 `person` 为 null
- 使用 Fail Fast 立即暴露问题

---

## 🎓 教训总结

### 1. 不要创建临时对象来传递数据

**错误模式：**
```csharp
Person temp = new Person();
temp.ID = -1;
temp.Name = null;
SomeMethod(temp);  // 临时对象导致问题
```

**正确模式：**
```csharp
SomeMethod(actualPerson);  // 传递实际对象
```

### 2. 方法签名应该反映实际需求

**错误签名：**
```csharp
void RecordEvent(string branchName, string p1, string p2)
{
    Person temp = new Person();  // 方法内部创建临时对象
}
```

**正确签名：**
```csharp
void RecordEvent(Person person, string branchName, string p1, string p2)
{
    // 使用传入的实际对象
}
```

### 3. 使用 Fail Fast 参数验证

**正确的参数验证：**
```csharp
if (person == null)
{
    throw new ArgumentNullException(nameof(person));
}
```

**不是 Band-Aid：**
- 这是 API 调用场景
- 调用者违反了契约
- 应该立即失败

---

**维护者：** Lead Architect  
**最后更新：** 2026-03-23  
**状态：** ✅ 修复完成，等待测试验证
